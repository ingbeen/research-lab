# research-lab 프로젝트 가이드라인

> CRITICAL: 특정 폴더의 파일을 분석하거나 작업할 때는 반드시 해당 폴더의 `CLAUDE.md` 를 먼저 읽고 참고합니다.
> 루트 문서는 프로젝트 전반의 공통 규칙을 제공하며, 각 폴더 문서는 해당 계층의 구체적인 맥락을 제공합니다.

## 문서 목적

이 문서는 AI 모델이 research-lab 프로젝트를 정확히 이해하고 일관성 있는 응답을 생성하도록 돕습니다.
사람을 위한 상세 문서가 아닌, **AI 모델의 판단 기준과 프로젝트 맥락**을 제공합니다.

---

## 이 프로젝트가 무엇인가 (CRITICAL)

research-lab 은 **무인으로 혼자 돌며 「이 매매법을 재볼 가치가 있는가」의 근거 문서를 만드는
파이프라인**입니다. 백테스트 도구도, 측정 도구도, 매매 도구도 아닙니다.

**만드는 것은 수익률이 아니라 «근거»입니다.** 재는 일은 다른 저장소가 합니다.

| 하는 일 | 하지 않는 일 |
| --- | --- |
| 웹에서 매매법 후보를 찾고 **반증을 함께** 모은다 | 백테스트 · 측정 · 통계 판정 |
| **출처가 독립인지** 재서 노이즈를 걷어낸다 | 매매 규칙 설계 · 손절 · 자금배분 |
| 데이터를 실제로 받을 수 있는지 · 개인이 집행 가능한지 판정한다 | 최적 파라미터 탐색 |
| **잴 수 있는 형태의 측정 설계 초안**을 써 준다 | 그 측정을 직접 수행 |

### 🔴 산출물 하나가 «독립»이어야 한다 — 1순위 제약

**dossier(근거 문서)는 이 저장소 밖으로 나갑니다.** 어느 프로젝트에든 ·
아직 존재하지 않는 신규 프로젝트에든 **그대로 넘길 수 있어야** 합니다.

> **다른 문서를 한 장도 열지 않고 판단할 수 있어야 한다.**

| 쓰지 않는다 | 대신 쓴다 |
| --- | --- |
| 「verify-lab 원칙 12 의 칸당 10건 하한에 걸린다」 | 「표본이 20건이라 시기를 둘로 쪼개면 칸당 10건이다. **칸당 10건 미만이면 우연과 구별되지 않으므로** 시기 분해를 할 수 없다」 |
| 「`storage/market/` 에 이미 있다」 | 「일봉만 필요하다. **국내 ETF 일봉은 pykrx 로 받을 수 있고**, 이 기계에 받아둔 것이 있다」 |
| 「기존 측정 계층을 쓰면 된다」 | 측정 설계를 **그 자리에 전부 적는다** |
| 「판정 기준은 그 저장소 규약을 따른다」 | **적용한 기준 자체를** dossier 안에 적는다 |

🔴 **「중복되니 빼자」는 판단을 하지 않습니다.** 가리키는 문서가 그 프로젝트에 없으면
dossier 는 그 순간 쓸모없는 종이가 됩니다 — **링크가 깨지는 게 아니라 판단이 불가능해집니다.**
그래서 **dossier 는 길어집니다. 의도한 것입니다.**

> **이것은 전역 규칙 「코드에서 파생 가능한 정보를 복제하지 않는다」의 «의도적 예외»입니다.**
> 그 규칙은 **한 저장소 안에 머무는 문서**를 전제합니다. dossier 는 전제가 다릅니다.
> 링크를 쓰는 쪽은 파이프라인 «안»의 것들(원장 · 로그 · `docs/`)이고 **dossier 만 예외입니다.**

### 🔴 이 저장소는 PUBLIC 이다 — 에이전트가 무인으로 쓴다

**자매 저장소에 없는 위험입니다.** 다른 저장소에서는 사람이 보는 앞에서 파일이 쓰이지만,
여기서는 **아무도 안 볼 때 회차마다** 쓰이고 그 결과가 공개됩니다.
실행 로그에는 검색어 · URL · 읽은 문서 경로 · 판단 근거가 그대로 들어갑니다.

- **자격증명은 어떤 형태로도 저장소에 들어오지 않습니다.** 토큰은 `~/.claude/keys/` 에
  권한 `600` 으로 두고 **환경변수로만** 컨테이너에 넘깁니다
- **`gate/secrets.py` 가 산출물과 로그를 스캔**해 자격증명 패턴이 있으면 그 회차를 실패로 만듭니다.
  `.gitignore` 는 **2차 방어일 뿐 1차가 아닙니다** — 규칙으로만 두면 새는 날이 옵니다
- 개인을 특정하는 정보 · 사적인 맥락을 dossier 나 로그에 적지 않습니다

### 🔴 추가 요금은 한 푼도 나오면 안 된다 — 인증 경로로 막는다

이 프로젝트는 **남는 구독 토큰**으로 돕니다. 과금 경로는 셋뿐이고 **전부 인증 문제**입니다.

| 경로 | 막는 법 |
| --- | --- |
| `ANTHROPIC_API_KEY` 가 설정돼 있음 | **실행 스크립트가 명시적으로 unset 하고, 설정돼 있으면 «시작을 거부»한다.** 있으면 구독 대신 API 과금으로 넘어간다 |
| 계정의 추가 사용량(extra usage) 크레딧 | 사용자가 claude.ai 설정에서 끈다. **코드로 확인할 수 없다** |
| 컨테이너에 API 키 주입 | 실행 스크립트가 **Claude 토큰 하나만** 넘긴다 |

> **`--max-budget-usd` 를 「과금 방지」로 쓰지 않습니다.** 그것은 **API 달러 지출의 상한**이고,
> 구독 인증에는 달러 지출 자체가 없습니다. **`0` 을 주면 파이프라인이 한 줄도 못 돌 수 있습니다.**
> 이 플래그는 **폭주 감지**용으로만 쓰며, 값은 「한 단계가 정상적으로 쓸 양보다 살짝 큰 값」입니다.
> 구독 인증에서 이 플래그가 동작하는지는 **[미검증]** 이며, 무시된다면 러너가 세션 로그의
> 토큰을 더해 직접 끊습니다.

**구독 인증은 한도를 다 쓰면 «그냥 멈춥니다».** 넘어가서 과금되지 않습니다.

---

## 산출물 — dossier 의 11칸

**자유 서술이 아니라 빈칸 채우기**입니다. 자유롭게 두면 **찾은 것만 쓰고 안 찾은 것은
언급조차 안 합니다.** 칸이 정해져 있으면 안 물을 수가 없습니다.

순서는 **「결론 먼저, 근거 뒤」** 입니다 — 나중에 읽는 사람이 위 다섯 칸만 보고 판단할 수
있어야 하고, 아래 여섯 칸은 그 판단을 의심할 때 내려가는 자리입니다.

| # | 칸 | 무엇을 적나 |
| --- | --- | --- |
| 1 | 한 줄 주장 | 무엇을 언제 사고 언제 파는가 |
| 2 | **판정** | 잴 가치 있음 / 보류 / 기각 **+ 반드시 이유 + 적용한 기준 자체** |
| 3 | 왜 우위가 있을 수 있나 | 리스크 프리미엄 · 행동 편향 · 제도 · 미시구조 |
| 4 | 데이터 실현가능성 | 필요한 데이터 · 있나 · 어떻게 받나 · 막히면 무엇으로 |
| 5 | 집행 현실성 | 살 수 있나 · 신호 빈도 · 배수 가능한가 · 분초 집행이 필요한가 |
| 6 | 출처 계보 | 누가 원본이고 누가 베꼈나 · **복제를 뺀 진짜 소스 수** |
| 7 | 찬성 근거 | URL · 발행일 · 1차/2차 구분 |
| 8 | **반증** | 반대 주장과 그 출처 — **별도 세션이 모은다** |
| 9 | 왜 사라졌을 수 있나 | 발표 후 소멸 · 제도 변화 · 시장 구조 변화 |
| 10 | 측정 설계 초안 | 그대로 잴 수 있는 형태로 **전부** 적는다 |
| 11 | 미검증 목록 | 확인 못 한 것 — **비는 게 오히려 의심스럽다** |

**문서의 순서와 «작업의 순서»는 다릅니다.** 채우는 순서는 `6 → 7 → 8 → 9 → 4 → 5 → 3`
이고 **2번은 맨 마지막에** 씁니다.

### 🔴 1번 칸은 «파라미터로 채울 수 있는» 것만 받는다 — 안 되면 후보가 아니다

**판정 기준은 하나입니다 — 「값을 하나 채우면 재는 절차가 확정되는가?」**

🔴 **「숫자가 들어 있는가」가 기준이 아닙니다.** 그 기준은 성질이 다른 둘을 같은 칸에 넣습니다.

| 갈래 | 예 | 왜 갈리나 | 처리 |
| --- | --- | --- | --- |
| **축은 있고 값이 비었다** | 짧은 기간 · 단기 · 며칠 전 · 크게 상회 · 고배당 · **전저점 대비** | **값을 채우면 재는 절차가 확정됩니다.** 격자로 스윕하면 됩니다 | **기각하지 않습니다** |
| 🔴 **축 자체가 없다** | 「**옥석을 가려**」 · 「유망한 것 중에」 | 무엇을 채울지조차 없습니다. **파라미터는 값이 빌 때 채우는 것이지 «무슨 값인지 모를 때»는 못 채웁니다** | **기각합니다** |
| 🔴 **판정 시점에 모르는 값** | 「**미래** 저점에서 산다」 · 「고점에서 판다」 | **미래 참조라 측정이 성립하지 않습니다.** 다만 「전저점」은 계산 가능하므로 **단어로는 둘이 안 갈립니다** | **기각합니다** |
| 🔴 **주체가 없는 예측** | 「편입이 **예상되는**」 | 누가 언제 무엇을 보고 예상하는지가 없으면 정보 집합이 정의되지 않습니다 | **기각합니다** |

**근거 둘.** ① §2 의 채워 본 예시가 이미 「진입 12월 20~31일 각 거래일 / 청산 1월 말일 기준
**격자**」로 돼 있습니다 — 범위를 격자로 받는 것이 **이미 이 파이프라인의 관용**입니다.
② 「며칠인가」를 물어 버리면 답을 모르는 에이전트가 **임의로 값 하나를 채웁니다.**
격자는 **값을 고르지 않고 축만 정하므로** 그 사고를 막습니다.

#### 그래서 정성 표현 사전은 «해명을 요구하는» 목록입니다

> **걸리면 버리는 것이 아니라, 그 표현에 대응하는 «파라미터 축과 후보값»을 내라고 요구합니다.**

| 한 줄 주장 | 내는 파라미터 | 판정 |
| --- | --- | --- |
| 「**짧은 기간** 내 동시 매수」 | `{동시 매수 판정 창, 거래일, [5, 10, 20]}` | **통과** |
| 「**전저점 대비** -10%」 | `{전저점 산정 일수, 거래일, [20, 60]}` | **통과** |
| 「**미래** 저점에서 재매수」 | 낼 수 있는 축이 없다 | **기각** |
| 「**옥석을 가려** 매수」 | `{옥석 기준, ["좋은 것"]}` — 숫자 후보값 2개 요건에 걸린다 | **기각** |

**이 구조의 핵심은 오탐의 대가가 거의 0 이라는 것입니다.** 사전에 멀쩡한 말이 섞여도
파라미터 한 줄이면 통과하므로 **사전을 넉넉히 키워도 좋은 후보가 죽지 않습니다.**

**게이트는 「적혔는가」만 봅니다** — 그 축이 옳은지 · 그 값이 판정 시점에 관측 가능한지는
판정하지 않습니다. 판정하려 들면 게이트가 또 하나의 판단자가 됩니다.

🔴 **기계가 못 막는 것 둘을 알고 씁니다** — ① 사전에 없는 정성 표현
② 「미래 저점」을 「전저점」인 척 포장한 격자. **둘 다 나중에 사람이 보는 자리입니다.**

> **축을 못 정하겠으면 그 후보를 버리는 것이 맞습니다.** 임의로 값을 채우면
> 5번 칸에 비용을 넣지 않는 것과 같은 사고가 납니다 — **어떤 값을 넣느냐가 결론을 만듭니다.**
> 버릴 때는 원장에 **사유와 함께** 남겨 다음에 또 파지 않게 합니다.
> 기각된 후보를 원장에서 «지우지» 않습니다 — 지우면 다음 탐색이 같은 후보를 새로 담고
> 그 회차가 또 기각합니다. **기각은 「본 적 없다」가 아닙니다.**

### 🔴 5번 칸에 비용 · 세금 · 슬리피지를 넣지 않는다

**케이스마다 다르고 단정할 수 없습니다.** 증권사 · 계좌 · 이벤트에 따라 자릿수가 달라지고,
그 폭이 기대값과 같은 크기라 **어떤 값을 넣느냐가 결론을 만듭니다.**

| 5번 칸에서 묻는 것 | 묻지 않는 것 |
| --- | --- |
| 살 수 있는 상품이 있나 | ~~수수료를 빼면 남나~~ |
| 신호가 얼마나 자주 오나 | ~~슬리피지 반영 후 성적~~ |
| 배수를 걸 수 있나 | ~~세금~~ |
| 분 · 초 단위 집행이 필요한가 | |

**예외는 하나 — 사용자가 별도로 요청할 때.** 그때는 ① 요청받은 값과 ② 그 출처,
③ **그 값이 결론을 어떻게 바꾸는지**를 dossier 안에 함께 적습니다.
셋 중 하나라도 빠지면 다시 「값이 판정을 대신하는」 상태가 됩니다.

### 게이트는 「존재」만 검사한다 — 내용은 검사하지 않는다

칸이 비면 그 회차를 **미완성**으로 기록합니다. 다만 **반증의 «질»을 판정하지 않습니다** —
그러면 게이트가 또 하나의 판단자가 됩니다. **빈 경우에도 「없음」이라 적을 길을 남깁니다.**

에이전트가 자기 점수를 매기지 않습니다. **러너(파이썬)가 산출물을 읽어 판정합니다.**

---

## 리서치의 규율 (CRITICAL)

**웹에서 합의는 근거가 아닙니다.** 여러 곳이 같은 말을 하는 가장 흔한 이유는 서로 베꼈기
때문이고, 그렇게 만들어진 합의는 「여러 소스에서 확인됨」이라는 라벨을 달고 옵니다.

| 규율 | 기계가 무엇을 하나 |
| --- | --- |
| **검색어를 갈아 끼운다** — 한/영, 업계어/일상어, 3회 이상 | 검색어 목록을 **파일로 남긴다.** 3개 미만이면 미완성 |
| **반증을 따로 검색한다** — `X 비판` · `X debunked` · `X doesn't work` | **반증 세션을 분리한다.** 찬성 근거를 모은 세션이 반증도 찾게 하지 않는다 |
| **소스의 독립을 잰다** — 같은 숫자 · 같은 예시가 반복되면 소스 수는 1 | 계보표를 만든다 (6번 칸) |
| **1차 출처까지 내려간다** | 2차 서술만 있으면 그 사실은 **「미검증」** 으로 적는다 |
| **날짜를 본다** | 글의 발행일과 그 글이 말하는 시점을 구분해 적는다 |

### 왜 반증을 «별도 세션»으로 떼나

같은 세션에서 찬성 근거를 20개 모은 다음 「이제 반증을 찾아라」라고 하면,
**이미 쌓인 맥락이 반증 탐색을 방해합니다.** 자기가 방금 지지한 것을 스스로 무너뜨리라는
요구이고 사람도 잘 못 합니다. **반증 세션은 「한 줄 주장」만 받고, 많이 찾을수록 잘한 것입니다.**

### 🔴 남는 위조 위험은 하나 — 없는 출처

백테스트가 없으니 부풀릴 점수가 없습니다. 대신 **존재하지 않는 논문 · URL 을 그럴듯하게
지어내는 것**이 남고, **읽어서는 구별되지 않습니다.**

**기계로 막습니다** — 게이트가 dossier 안의 **모든 URL 을 실제로 호출**해 살아 있는지
확인하고, 죽은 URL 이 하나라도 있으면 그 회차를 미완성으로 만듭니다. 사람이 판단할 일이 아닙니다.

**실물 사례** — 설계 단계에서 이미 한 번 잡혔습니다. 「정책이 7월부터 시행 중」이라는
검색 요약이 나왔는데, **근거 기사의 발행일이 두 달 전이었고** 그 기사의 「6월부터 시행된다」는
**미래형 문장을 검색 엔진이 현재형으로 합성**한 것이었습니다. 1차 출처로 내려가야만 갈렸고,
**에러는 전혀 나지 않았습니다.**

---

## 상태와 로그 (CRITICAL)

### 상태의 진실은 대화가 아니라 «파일»에 둔다

단계 사이를 오가는 것이 대화가 아니라 **파일**이라서, 한도로 끊겨도 · 컨테이너가 죽어도 ·
PC 가 재부팅돼도 같은 방식으로 복구됩니다.

- **임시 파일에 쓰고 성공했을 때만 rename** 합니다. 반쯤 쓴 문서가 완성본 자리에 남지 않게
- **회차의 첫 단계는 새 후보가 아니라 「지난 회차의 미완성」** 입니다. 순서가 반대면
  **아무도 모르는 채 미완성만 쌓입니다.** 이 순서 덕에 미완성은 **구조적으로 최대 1개**입니다
- 같은 미완성이 N번 연속 실패하면 **「막힘」으로 사유와 함께 원장에 박고** 넘어갑니다

### 회차는 「한 장 만들고 끝」이 아니라 «돕니다»

**남는 토큰을 쓰는 것이 이 프로젝트의 목적**이라, 일찍 끝났다고 멈추면 목적과 어긋납니다.

| 상황 | 어떻게 |
| --- | --- |
| 한 장을 끝냈는데 예산이 넉넉하다 | **다음 후보로 간다** |
| 미완성을 이어받아 **조금 쓰고 완성**했다 | 같다. 남은 예산으로 새 후보를 시작한다 |
| 한 장을 새로 파기엔 모자라다 | **탐색으로 전환** — 후보 목록만 채운다. 훨씬 싸다 |
| 탐색조차 못 할 만큼 적다 | 종료. **남은 예산을 로그에 적는다** |

**깊이는 줄이지 않습니다.** 장수를 늘리는 것이지 칸을 나누는 것이 아닙니다 —
**예산이 모자라면 장수를 줄이지 칸을 줄이지 않습니다.** 칸을 줄이면 그건 미완성입니다.

### 모든 단계가 「왜 그렇게 판단했는지」를 남긴다

산출물(dossier)은 **결론**이고, 로그는 **과정**입니다. 둘을 섞으면 dossier 가 읽히지 않습니다.

| 로그에 적는 것 | 없으면 나중에 못 하는 일 |
| --- | --- |
| **무엇을 읽었나** — 검색어 전부 · 연 URL 전부 · 읽은 문서 경로 | 「이 결론이 어디서 왔지」를 되짚을 수 없다 |
| **무엇을 기준으로 판단했나** | 기준을 고쳤을 때 **어느 과거 판정이 영향받는지** 모른다 |
| **무엇을 버렸고 왜** | 다음에 같은 후보를 또 파면서 같은 것을 또 버린다 |
| **분류 못 한 실패의 원문** | 처음 한도에 부딪히는 날 그 답을 못 얻는다 |
| 토큰 · 비용 · 소요 시간 | 회차 예산을 정할 근거가 없다 |

**줄글이 아니라 구조화(JSONL)로 남깁니다** — 「나중에 기계가 훑을 것」이기 때문입니다.
게이트 기준을 바꿨을 때 **과거 로그를 다시 읽어 「지금 기준이면 판정이 달라졌을 후보」를
찾아낼 수 있어야** 합니다.

### 🔴 「누적 집계」를 만들지 않는다

판정 기준은 하나입니다 — **「하루 걸렀을 때 영구 손상인가?」**

| 만들 것 | 하루 걸렀을 때 | 판정 |
| --- | --- | --- |
| 원장 (후보 목록) | 그 회차가 없었을 뿐. append-only | **만든다** |
| 미완성 표시 | 다음 회차가 이어받는다 | **만든다** |
| 결정 로그 | 그 회차의 로그가 없을 뿐 | **만든다** |
| ~~완주율 · 성공률 누적 집계~~ | **틀어지고, 틀어진 걸 아무도 모른다** | **만들지 않는다** |
| ~~후보별 점수 누적~~ | 같음. 게다가 「점수」는 또 하나의 판단자다 | **만들지 않는다** |

평균 비용처럼 집계가 필요하면 **그때 로그를 훑어 계산합니다.**

---

## 실패를 셋으로 가른다

| 갈래 | 무엇 | 어떻게 |
| --- | --- | --- |
| **① 한도 소진** | 5시간 · 주간 한도 | 그 단계를 표시하고 **깨끗이 종료.** 다음 회차가 이어받는다. **재시도하지 않는다** |
| **② 인증 · 과금 거부** | 토큰 만료 · 정책 변경 · 결제 실패 | **사람에게 알리고 정지.** ①·③과 섞이면 **며칠 조용히 안 도는 상태**가 된다 |
| **③ 그 외** | 네트워크 끊김 · 웹 500 · JSON 깨짐 · **분류에 실패한 미지의 실패 전부** | **횟수 상한까지 재시도**, 안 되면 그 단계만 포기하고 다음 회차로 |

🔴 **상한이 없으면 한 회차 내내 같은 실패를 반복하며 토큰을 태웁니다.** 나중에 보면
**예산은 다 썼고 산출물은 0장**이고, 그런 회차는 「실패」가 아니라 「아무 일 없음」처럼 보여
**며칠 지나서야 알아챕니다.**

**모르는 실패를 ③으로 떨어뜨리는 것이 핵심입니다.** 분류기를 완벽하게 만드는 대신
**모를 때 안전한 쪽으로 떨어지게** 만듭니다. 원문은 로그에 남아 다음에 가르칠 수 있습니다.

---

## 실행 환경

| 항목 | 값 | 이유 |
| --- | --- | --- |
| 컨테이너 | Docker · **`--user 1000:1000`** | 안 걸면 root 소유 파일이 생겨 호스트에서 못 고친다 |
| 베이스 이미지 | **멀티아치만** (`python:3.12-slim`) | 개발은 WSL(amd64), 운용은 mac(arm64) |
| 컨테이너 안 경로 | **`/work` 로 고정** · 호스트 경로는 바깥에서 주입 | 코드에 박으면 `/home/...` 과 `/Users/...` 가 갈린다 |
| 저장소 | **bind mount** | 산출물이 호스트 폴더에 그대로 생긴다. 컨테이너 안에 상태를 두지 않는다 |
| 전역 `~/.claude` | **마운트하지 않는다** | 마운트하면 PC 마다 훅·스킬이 달라 **재현이 깨진다.** 리서치 스킬은 이 저장소가 소유한다 |
| 권한 모드 | `bypassPermissions` | 무인 실행에서 승인창이 뜨면 프로세스가 그냥 멈춘다 |

> **컨테이너는 «보안 샌드박스가 아닙니다».** 리서치는 인터넷이 필수라 그 조건을 못 맞춥니다.
> 컨테이너가 막는 것은 **파일 시스템**이지 네트워크가 아닙니다.
> 그래서 **Claude 토큰 외에 어떤 자격증명도 넣지 않습니다.**

### 트리거 등록은 «제가» 합니다 — 전역 규칙의 이 저장소 한정 예외 (2026-09-15)

전역 규칙 「환경 설치는 사용자가 직접 한다 — 가이드를 쓰고 기다린다」의 Why 는
**무엇이 이 기계에 «설치»되는지를 쓰는 사람이 통제하는 것**입니다.
**트리거 등록은 아무것도 설치하지 않습니다** — 이미 문서에 적힌 설정을 적용할 뿐이라
그 Why 가 걸리지 않고, 매번 사용자에게 넘기면 **예약 시각 한 번 바꾸는 데 왕복이 생깁니다.**

| 제가 한다 | 사용자가 한다 |
| --- | --- |
| 작업 정의 파일 편집 · `launchctl bootout`/`bootstrap` · 등록 뒤 읽기 전용 확인 | **`sudo` 가 필요한 것** (절전 해제 예약) |
| 서비스 등록 (`brew services start` 류) | **패키지·런타임 설치** (엔진 · Python · Poetry) |

**절차를 문서로 남기는 것은 그대로입니다** — 기계를 옮기는 날 그 문서가 필요하고,
거기 담긴 것은 「누가 눌렀나」가 아니라 **무엇을 왜 그렇게 등록하나**입니다.

### 개발은 WSL · mac 을 오간다 — 싱크는 git 이 맡는다

- **git 제외**: `.venv/` · `__pycache__/` · 이미지 · 컨테이너 내부 상태 — 아치가 다르면 깨진다
- **git 포함**: `Dockerfile` · lock 파일 · **리서치 스킬** · 원장 —
  이미지는 git 에 안 올라가므로 **Dockerfile + lock 이 재현의 전부**다
- **다른 저장소에서 poetry 를 부를 때는 `env -u VIRTUAL_ENV` 를 붙인다** —
  세션을 시작한 저장소의 venv 가 셸에 남아 있어, 안 붙이면 엉뚱한 venv 를 써서
  `ModuleNotFoundError` 가 나고 **「패키지가 없다」로 오진한다**

---

## 다른 저장소와의 관계

| 저장소 | 관계 |
| --- | --- |
| **verify-lab** | 이 파이프라인이 만든 의뢰서를 **받아서 재는 쪽 중 하나**. 다만 **dossier 는 verify-lab 전용이 아니다** — 어느 프로젝트에든 넘어간다 |
| **quant** | 백테스트 전용. 이 저장소와 직접 주고받는 것은 없다 |
| **quant-notify** | 알림 전용. **「무상태」 설계의 선례**이며 「누적 집계를 만들지 않는다」가 거기서 왔다 |

**들어오는 지식과 나가는 산출물을 헷갈리지 않습니다.**

| 방향 | 링크 써도 되나 |
| --- | --- |
| **들어옴** — 데이터 카탈로그 등 리서치가 참고하는 지식 | **된다.** 파이프라인 안에 머문다 |
| **나감** — dossier | **안 된다.** 저장소 밖으로 나가 포인터가 전부 죽는다 |

---

## 계획서(Plan) 작성이 필요한 경우

원칙: **모든 코드 변경은 계획서를 작성한 후 진행합니다.**

예외 (계획서 없이 바로 진행 가능): 오타 수정 · 주석 수정 · 로그 메시지 수정.

계획서 작성 절차 및 품질 게이트: **전역 `/impl-plan` 스킬**(`~/.claude/skills/impl-plan/SKILL.md`)이 SoT입니다.

이 규칙은 하네스가 강제합니다 (전역 `~/.claude/hooks/`).

- 문서(`docs/**`, `**/*.md`)를 제외한 파일을 편집할 때 계획서가 선행되지 않으면 권한 프롬프트가 뜹니다
- Done 조건을 위반한 계획서는 저장이 차단됩니다

### 계획서의 수명 (CRITICAL)

계획서는 **작업 중에만 존재하는 임시 산출물**입니다. 주기적으로 전부 비웁니다.

- **살아있는 문서에서 계획서를 링크하지 않습니다.** 링크하면 다음 삭제 주기에 깨집니다
- **Done 처리 시 남길 가치가 있는 것을 살아있는 문서로 승격합니다.**
  결론뿐 아니라 **근거**를 옮깁니다 — 「값이 X다」가 아니라 「왜 X인가」
- **`docs/plans/` 폴더는 비어도 유지합니다** (`.gitkeep`). 게이트 훅이 이 폴더의 존재로
  규약 채택을 판정하므로, 사라지면 게이트가 **조용히 꺼집니다**

### 계획서 규약 — 이 프로젝트의 설정 (CRITICAL)

#### 1. 검증 명령

| 용도 | 명령 | 실행 시점 |
| --- | --- | --- |
| 품질 검증 | `poetry run python validate_project.py` | **마지막 Phase에서만** |
| 자동 포맷 | `poetry run black .` | 마지막 Phase에서 **적용만** (`--check` 는 쓰지 않습니다) |

#### 2. 근거 승격 목적지

| 옮길 것 | 목적지 |
| --- | --- |
| 설계 결정 · **탈락안과 그 이유** · 실측 기록 | `docs/DESIGN.md` |
| 계층 간 계약 | `src/research_lab/CLAUDE.md` |
| 실행 명령어 | `docs/COMMANDS.md` |

> **`docs/spec/` 을 두지 않습니다.** 이 저장소는 검증이 여럿이 아니라 **파이프라인 하나**라,
> 나누면 어디에 적을지가 매번 판단 대상이 됩니다.

#### 3. 커밋 메시지의 기능명

`기능명 / 내용` 형식을 쓰며, 기능명은 **변경된 파일 경로**로 정합니다.

| 경로 | 기능명 |
| --- | --- |
| `src/research_lab/runner/` | `러너 / ` |
| `src/research_lab/agent/` | `에이전트 / ` |
| `src/research_lab/gate/` | `게이트 / ` |
| `.claude/` · `Dockerfile` · `docker/` · 설정 파일 | `하네스 / ` |
| 문서 | `문서 / ` |

후보 생성은 `/commit` 스킬을 씁니다.

---

## 세션 시작 규칙

| 순서 | 읽을 것 | 왜 |
| --- | --- | --- |
| 1 | `CLAUDE.md` (이 문서) | 자동 로드 |
| 2 | [docs/DESIGN.md](docs/DESIGN.md) | **확정 설계 · 탈락안 · 실측 기록의 SoT.** 왜 이렇게 만들었는지가 전부 여기 |
| 3 | 작업 대상의 `docs/plans/PLAN_*.md` | 지금 무엇을 하는 중인지 |
| 4 | `src/research_lab/CLAUDE.md` | 패키지 코드를 건드릴 때 — 계층 간 계약 |
| 필요할 때 | [docs/INDEX.md](docs/INDEX.md) | **문서 지도.** 위 넷에 없는 문서를 찾을 때. `tests/test_index.py` 가 실제 파일과 대조하므로 문서를 추가·이동하면 함께 고칩니다 |

- **진행 상태 문서와 인계 문서를 두지 않습니다.** 무엇이 끝났는지는 git 이력과 계획서가
  말하고, 무엇을 다음에 할지는 그때 사용자가 정합니다
- **경로 한정 규칙(`.claude/rules/`)과 하위 `CLAUDE.md` 는 `Read` 도구로 열었을 때만
  주입됩니다.** `cat`·`head`·`sed -n` 으로 읽으면 따라오지 않습니다 —
  **코드 파일은 `Read` 도구로 엽니다.** 셸은 검색·집계에 씁니다

---

## 실행 명령어 관리 원칙

> CRITICAL: 모든 실행 명령어(`poetry run` 등)는 **[docs/COMMANDS.md](docs/COMMANDS.md) 에서
> 단일 관리**합니다. `README.md` 와 `CLAUDE.md` 에는 실행 명령어를 기재하지 않습니다.
> 설치 시 한 번만 쓰는 일회성 명령어는 기재 대상이 아닙니다.

---

## 기술 환경

- Python 3.12 (`str | None` 문법 사용)
- 의존성 관리: Poetry (가상환경은 프로젝트 내부 `.venv`)
- 코드 품질: Black, Ruff · 타입 체커: PyRight (strict mode for `src/`)

---

## 이 프로젝트의 개발 원칙

전역 「개발 원칙」(YAGNI · 간결성 · 재사용성 · 영향도 관리 · 자문 체크 · 사용자 중심)에 더해
research-lab 고유로 둘을 더 봅니다.

- **기계가 지키게 한다**: 프롬프트로 지시한 규율은 **형식적으로만 지켜집니다.**
  반증 · 미검증 · 자격증명처럼 빠지면 치명적인 것은 **러너가 검사**합니다
- **모를 때 안전한 쪽으로 떨어진다**: 분류기 · 게이트 · 예산 판정 모두,
  **판정에 실패했을 때 파이프라인이 폭주하지 않는 쪽**을 기본값으로 둡니다.
  검사기가 죽어서 파이프라인을 멈추게 해서도 안 됩니다 — **판정을 못 하는 것**과
  **실패로 판정하는 것**은 다르고, 전자는 로그에 남기고 통과시킵니다
