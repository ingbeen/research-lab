# 다음 로드맵 단계 착수 프롬프트

> **쓰는 법**: 새 세션에서 이 문서 하나만 첨부하고 「이대로 진행해 줘」라고 하면 됩니다.
> 나머지는 이 문서가 시킵니다.

---

## 🔴 이 문서가 «진행 상태»를 담지 않는 이유

루트 `CLAUDE.md` 는 **「진행 상태 문서와 인계 문서를 두지 않습니다」** 라고 못박고 있습니다.
그래서 이 문서에는 **「지금까지 무엇이 끝났다」가 한 줄도 없습니다.**

무엇이 끝났는지는 **[DESIGN.md](DESIGN.md) §10 의 로드맵 표**가 말하고, 그 표가 SoT 입니다.
여기는 **절차만** 담으므로 Phase 2 든 5 든 **같은 문서를 그대로** 씁니다 —
갱신할 것이 없으니 낡지도 않습니다.

---

## 1. 먼저 읽는다 (순서대로)

| # | 문서 | 여기서 얻을 것 |
| --- | --- | --- |
| 1 | 루트 `CLAUDE.md` | 자동 로드. 산출물 독립성 · PUBLIC 제약 · 11칸 · 리서치 규율 |
| 2 | [DESIGN.md](DESIGN.md) **§10 로드맵** | **아직 안 된 «첫» 줄이 이번에 할 일이다** |
| 3 | [DESIGN.md](DESIGN.md) **§10.1 알려진 개선거리** | 그 Phase 가 «함께» 들고 갈 것이 적혀 있다 |
| 4 | [DESIGN.md](DESIGN.md) §11 · §11.1 | 확정된 결정과 실측치. **여기 있는 것을 다시 정하지 않는다** |
| 5 | [../src/research_lab/CLAUDE.md](../src/research_lab/CLAUDE.md) | 계층 간 계약 — 코드를 건드리기 전에 |

> **§11.1 을 건너뛰지 마세요.** CLI 플래그·컨테이너·실패 분류·응답 모양이 **실측으로**
> 적혀 있습니다. 안 읽으면 이미 답이 난 것을 다시 재게 됩니다.

---

## 2. 무엇을 할 단계인지 정한다

**[DESIGN.md](DESIGN.md) §10 의 로드맵 표에서 «아직 안 된 첫 줄»** 이 이번 단계입니다.
그 줄의 **「검증 방법」 칸이 곧 그 단계의 완료 조건**입니다 — 그것을 계획서의 DoD 로 옮깁니다.

**§10.1 에서 그 Phase 가 맡기로 한 개선거리를 같이 봅니다.** 거기 적힌 것은 「나중에」가
아니라 **그 Phase 의 일**입니다.

---

## 3. 계획서를 쓴다

**전역 `/impl-plan` 스킬이 절차의 SoT 입니다.** 그대로 따릅니다.

- 파일: `docs/plans/PLAN_<short_name>.md`
- 이 저장소의 값(검증 명령 · 근거 승격 목적지 · 커밋 기능명)은 **루트 `CLAUDE.md` 의
  「계획서 규약 — 이 프로젝트의 설정」** 절에 있습니다
- **코드를 고치기 전에 계획서를 승인받습니다.** 훅이 강제합니다

### 계획서에 반드시 넣을 것

| 항목 | 왜 |
| --- | --- |
| **비목표** | 안 적으면 다음 세션이 「빠졌다」고 오판하거나 없는 요구를 만들어 냅니다 |
| **[미검증] 목록과 «어떻게 잴 것인가»** | 모르는 채로 짓되 계측을 심는 것이 이 저장소의 방식입니다 |
| **계약 테스트를 Phase 0 에 먼저** | 레드 허용. 「무엇이 맞는가」를 구현 전에 고정합니다 |

---

## 4. 이 저장소에서 특히 지킬 것

### 🔴 기계가 지키게 한다

**프롬프트로 지시한 규율은 형식적으로만 지켜집니다.** 이 저장소에서 실제로 세 번 확인됐습니다 —
「다른 말 없이 JSON 만」이라 적었는데 코드펜스로 감쌌고, 검색어를 「3회 이상」이라 적었는데
검사가 없어 아무도 안 봤고, 스킬이 「러너가 검사한다」고 적었는데 **그 검사가 없었습니다.**

> **빠지면 치명적인 것은 러너·게이트가 검사하게 만듭니다.** 문서에 적는 것으로 끝내지 않습니다.

### 🔴 모를 때 안전한 쪽으로 떨어뜨리고 «원문»을 남긴다

실패 분류표가 **세 번 다 빗나갔지만**(인증·예산·한도) 셋 다 복구됐습니다 —
원문을 통째로 남겨 뒀기 때문입니다. 새로 만드는 분류·게이트·판정도 같게 짭니다.

### 🔴 산출물(dossier)은 다른 문서를 가리키지 않는다

**1순위 제약입니다.** 저장소 밖으로 나가면 포인터가 전부 죽습니다.
「그 저장소 규약대로」가 아니라 **적용한 기준 자체를** dossier 안에 적습니다.

### 🔴 한 줄 주장은 정량화된 것만

「크게 상회」·「저점에서」·「옥석을 가려」는 **잴 수 없습니다.**
판정 기준은 「이 문장만 보고 두 사람이 같은 날 같은 종목을 고를 수 있는가」입니다.
자세한 것은 루트 `CLAUDE.md` 의 「1번 칸은 «정량화»된 것만 받는다」 절에 있습니다.

### 이 저장소는 PUBLIC 이고 에이전트가 무인으로 씁니다

자격증명은 어떤 형태로도 들어오지 않습니다. 개인을 특정하는 정보도 적지 않습니다.

---

## 5. 하지 않는 것

| 하지 않는다 | 왜 |
| --- | --- |
| **git 명령** | 사용자가 직접 합니다 (전역 규칙) |
| **진행 상태·인계 문서 만들기** | 이 저장소가 두지 않기로 한 것입니다 |
| **누적 집계 만들기** | 「하루 걸렀을 때 영구 손상인가」로 판정합니다. 완주율·점수는 만들지 않습니다 |
| **§11 에서 이미 정해진 것 다시 정하기** | 탈락안과 이유가 적혀 있습니다. 뒤집으려면 **왜 뒤집는지를 먼저** 적습니다 |
| **아티팩트로 산출물 내기** | 산출물은 파일입니다 |

---

## 6. 끝낼 때

`/impl-plan` 의 Done 조건을 따릅니다. 그중 **기계가 검사하지 않는 것 하나**를 특히 챙깁니다.

> **근거 승격** — 계획서는 주기적으로 전부 삭제됩니다.
> **「이 계획서를 지금 지워도 잃는 것이 없는가?」** 아니라면 아직 옮길 것이 남았습니다.
> 옮기는 것은 결론뿐 아니라 **근거**입니다 — 「값이 X다」가 아니라 「왜 X인가」.
> 목적지는 루트 `CLAUDE.md` 의 「근거 승격 목적지」 표에 있습니다.

그리고 **로드맵 표와 §10.1 을 갱신합니다** — 끝낸 줄에 완료 표시를 하고,
이번에 새로 드러난 개선거리를 §10.1 에 **어느 Phase 가 맡을지와 함께** 적습니다.
적어 두지 않으면 「나중에」는 영영 오지 않습니다.
