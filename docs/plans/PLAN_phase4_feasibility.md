# Implementation Plan: 로드맵 Phase 4 — 실현가능성 (데이터 카탈로그 + dossier 4·5번 칸)

> 작성/운영 규칙(SoT): `/impl-plan` 스킬(`~/.claude/skills/impl-plan/SKILL.md`)을 반드시 참고하세요.  
> (이 템플릿을 수정하거나 새로운 양식의 계획서를 만들 때도 해당 스킬을 포인터로 두고 준수합니다.)

**상태**: ✅ Done

---

🚫 **이 영역은 삭제/수정 금지** 🚫

**상태 옵션**: 🟡 Draft / 🔄 In Progress / ✅ Done

**Done 처리 규칙**:

- ✅ Done 조건: DoD 모두 [x] + `skipped=0` + `failed=0`
- ⚠️ **스킵이 1개라도 존재하면 Done 처리 금지 + DoD 테스트 항목 체크 금지**
- 상세: `/impl-plan` 스킬의 "3) 스킵 및 완료 규칙" 참고
- 위 조건은 `~/.claude/hooks/plan_lint.py`가 저장 시 자동 검사합니다

---

**작성일**: 2026-09-14 11:47
**마지막 업데이트**: 2026-09-14 13:50
**관련 범위**: 러너(밤의 다섯째 단계) · 게이트 · 리서치 스킬 · 문서(데이터 카탈로그)
**관련 문서**: `CLAUDE.md`, `docs/DESIGN.md`, `src/research_lab/CLAUDE.md`, `docs/INDEX.md`, `.claude/skills/night-research/SKILL.md`

---

## 0) 고정 규칙 (이 plan은 반드시 아래 규칙을 따른다)

> 🚫 **이 영역은 삭제/수정 금지** 🚫
> 이 섹션(0)은 지워지면 안 될 뿐만 아니라 **문구가 수정되면 안 됩니다.**
> 규칙의 상세 정의/예외는 반드시 `/impl-plan` 스킬을 따릅니다.

- 품질 검증 명령은 **마지막 Phase에서만 실행**한다. 실패하면 즉시 수정 후 재검증한다.
- Phase 0은 "레드(의도적 실패 테스트)" 허용, Phase 1부터는 **그린 유지**를 원칙으로 한다.
- 이미 생성된 plan은 **체크리스트 업데이트 외 수정 금지**한다.
- 스킵은 가능하면 **Phase 분해로 제거**한다.

---

## 1) 목표(Goal)

- [x] 목표 1: **데이터 카탈로그를 이 저장소에 들여온다** — 「무엇을 받을 수 있나」만 요약하고, 항목마다 **자립 서술 · 함정 · 원본 포인터**를 담는다. 4번 칸을 매번 새로 알아내면 낭비이고 **더 나쁘게는 틀리게 알아낸다**
- [x] 목표 2: **밤에 다섯째 단계 `feasibility` 를 붙여 dossier 4·5번 칸을 채운다.** 러너가 카탈로그 본문을 프롬프트에 실어 보내므로 읽힘이 구조적으로 보장된다
- [x] 목표 3: **4·5번 칸의 필수 자리가 비면 막는 게이트를 만든다** — 「적혔는가」만 본다. 비용·세금·슬리피지 혼입은 **막지 않고 계측만** 남긴다
- [x] 목표 4: **옵션 만기일 후보로 밤을 완주시켜 데이터가 「이미 있음」으로 나오는 것을 실측하고**, 같은 밤에 §10.1 F(반증·계보 자리의 강제 종료 이어받기)를 닫는다
- [x] 목표 5 **[2026-09-14 추가 · 사용자 승인]**: 🔴 **강제 종료 뒤 잠금이 남아 이어받기가 «구조적으로» 안 되는 것을 고친다.** F 를 재려고 절차를 짜다 그 자리에서 나온 것이고, **고치지 않으면 F 를 아예 잴 수 없다** — 「다시 돌려 `[이어받기]` 가 뜨는지 본다」가 일어나지 않는다. 원장 잠금까지 남아 **밤이 한 번 죽으면 이후 모든 밤이 정지**한다

## 2) 비목표(Non-Goals)

- **2번 칸(판정) · 3 · 9 · 10 · 11번 칸** — Phase 5 의 「11칸 전체」가 맡는다. 이번 단계는 **4·5번 칸만** 낸다
- **dossier 마크다운 파일 생성** — Phase 5 다. 이번 산출물은 후보 폴더의 `실현가능성.json` 한 장이다
- **런타임 의존성 추가** — §10 의 「여전히 0개」를 이 Phase 도 깨지 않는다. 카탈로그는 본문을 통째로 실어 보내므로 파싱 라이브러리가 필요 없고, `id` 추출은 `re` 로 끝난다
- **`--json-schema` 로 응답 모양 강제** — Phase 5 가 11칸 전체에 한꺼번에 건다. 지금 4·5번 칸에만 걸면 Phase 5 에서 두 번 이주시킨다
- **비용 · 세금 · 슬리피지를 5번 칸에 넣기** — 루트 `CLAUDE.md` 와 DESIGN §2 의 금지다. **게이트로 막지도 않는다** (아래 「확정한 설계 결정」 D3)
- **feasibility 가 후보를 기각하기** — 4·5번 칸은 칸을 채우는 자리이고 기각은 판정(2번 칸)이다. 여기서 기각을 내면 게이트가 또 하나의 판단자가 된다
- **§10.1 C(정성 표현마다 축 대응) · D(반증의 몰래 읽기) · G(URL 게이트가 못 보는 두 자리)** — 표가 Phase 5 로 지정한 것들이다
- **데이터를 실제로 받는 코드** — 이 저장소는 재지 않는다. 카탈로그는 「받을 수 있나」만 말한다
- **원장에서 후보를 꺼내는 순서 변경** — DESIGN 「아직 안 정한 것」 1 이 ①(쌓인 순서)로 두기로 한 것이다. 실측을 위해 후보 한 줄을 위에 넣는 것은 **사람이 원장을 손으로 고치는 것**이고 정책 변경이 아니다
- **미국 축 카탈로그의 전수 정리** — 옵션 만기일 DoD 가 요구하는 만큼(미국 ETF 일봉 · 수정주가 교차검증)만 담는다

## 3) 배경/맥락(Context)

### 현재 문제점 / 동기

- 밤은 지금 네 단계(`explore → collect → rebut → lineage`)로 **찬성 근거 · 반증 · 출처 계보까지** 낸다. 그런데 **4번 칸(데이터 실현가능성)과 5번 칸(집행 현실성)이 통째로 없다.**
- DESIGN §2 의 채워 본 예시에서 **문제를 실제로 잡아낸 칸이 5번**이었다 — 「신호가 연 1회」는 읽자마자 걸리는데 **근거만 모으는 문서였다면 안 나왔을 항목**이다. 지금 파이프라인이 딱 그 상태다
- 4번 칸을 카탈로그 없이 채우면 **에이전트가 매번 새로 알아내고, 더 나쁘게는 틀리게 알아낸다**(DESIGN §5.1). 이미 실측으로 밝혀진 것이 자매 저장소에 흩어져 있다
- 🔴 **카탈로그는 「들어오는 지식」이라 링크를 써도 되지만, 산출물(dossier)은 안 된다.** 4번 칸에 「카탈로그에 있다」로 적히면 dossier 가 저장소 밖에서 **판단 불가능한 종이**가 된다 — 이 저장소의 1순위 제약이다

### 영향받는 규칙(반드시 읽고 전체 숙지)

> 아래 문서에 기재된 규칙을 **모두 숙지**하고 준수합니다.

- 루트 `CLAUDE.md` — 특히 「계획서 규약 — 이 프로젝트의 설정」 절
- 전역 `~/.claude/CLAUDE.md`
- 전역 `~/.claude/rules/python.md`
- [../DESIGN.md](../DESIGN.md) — §2 · §5 · §5.1 · §5.2 · §7 · §7.0 · §10 · §10.1 · §11 ~ §11.4
- [../../src/research_lab/CLAUDE.md](../../src/research_lab/CLAUDE.md)
- [../../.claude/skills/night-research/SKILL.md](../../.claude/skills/night-research/SKILL.md)
- [../INDEX.md](../INDEX.md)

### 이 계획서를 쓰기 전에 «확인한» 것 (추측이 아니다)

| 확인한 것 | 결과 |
| --- | --- |
| 카탈로그 씨앗 4건이 이 기계에 실재하나 | **전부 있다** — `verify-lab/reference/pykrx_실측기록.md`(459줄) · `docs/spec/` 의 레버리지 ETF 괴리(300) · 선물 대 레버리지 ETF(456) · 옵션 만기일(506) |
| 시세 파일이 실제로 받아져 있나 | **있다** — `verify-lab/storage/market/` 에 **58개 파일**. 국내 ETF(069500 등) · **미국 ETF(QQQ · SPY · DIA · TQQQ 등)** · 국내 선물(KRDRVFUK2I · KRDRVFUKQI), **원본가와 수정주가 쌍** |
| 🔴 DESIGN §7.0 의 「미국 쪽은 아직 실측이 없다」 | **사실과 어긋난다.** 미국 ETF 일봉은 yfinance 계열로 받아 **받아둔 것이 있고**, 수정주가 교차검증 실측도 옵션 만기일 설계 §7.6 에 있다. 정정이 필요하다 (아래 Notes) |
| 옵션 만기일 데이터가 「이미 있음」으로 닫히나 | **닫힌다.** 만기일은 외부에서 받는 데이터가 아니라 **달력 규칙**이고, 휴장 앞당김 판정에 필요한 거래일 목록은 **시세 파일의 날짜 인덱스 자체**다 (실측 2026-08-27) |
| 카탈로그에 담아야 하는 함정이 있나 | **있다.** pykrx 수정주가는 조회 시점 기준 최근 약 3,000거래일만 돌아오고 **그 시작점조차 고정이 아니다**(2026-08-12 조회 → 2014-05-22 시작 / 2026-08-27 조회 → 2014-06-09 시작) |
| `common_constants.DOCS_DIR` 의 소비자 | **0건.** `tests/test_gate_secrets.py:156` 이 「스캔 대상이 아니다」로 쓰는 것이 유일한 참조다. 카탈로그 경로가 이 상수의 **첫 실사용처**가 된다 |
| `steps.STEPS` 를 늘려도 되나 | 늘릴 수 있으나 **예전 상태 파일 처리를 함께 정해야 한다** — `steps.py` 의 상수 주석과 `UnknownStepError` 가 그것을 요구하고, Phase 2 가 실제로 그 자리에서 한 번 겪었다(§11.2) |
| 완주한 예전 실행 폴더가 몇 개인가 | **2개** — `runs/20260912_1444` · `runs/20260912_2034`. 둘 다 `settled` 가 네 단계라 STEPS 를 늘리면 **「남은 단계 있음」으로 보인다** |
| 한 밤 실측 비용 | **$2.24** (수집 $0.79 / 202초 · 반증 $0.98 / 273초 · 계보 $0.47 / 172초) — §11.2 |
| 실측용 후보의 한 줄 주장이 정성 표현 게이트에 걸리나 | **안 걸린다.** 「만기주 첫 거래일에 사서 만기일 종가에 판다」는 달력만 보면 값이 정해지고, `gate/quantified.QUALITATIVE_TERMS` 의 어느 항목도 걸리지 않는다 |

### 확정한 설계 결정 넷 (2026-09-14, 사용자 승인)

| # | 결정 | 근거 |
| --- | --- | --- |
| **D1** | **`feasibility` 를 밤의 «마지막» 단계로 두고 「판 것」 표시를 `lineage` → `feasibility` 로 옮긴다** | 채우는 순서가 `6→7→8→9→4→5→3` 이라 4·5는 계보·찬성·반증 **뒤**다. 표시를 옮기지 않으면 계보가 표시한 뒤 feasibility 가 실패할 때 **후보가 4·5번 칸 없이 「판 것」으로 남아 영영 다시 안 파진다** — §11.2 가 경고한 그 모양이다. 「마지막 단계가 표시한다」는 계층 계약 §4 가 이미 이것을 요구하고, Phase 2 가 `collect → lineage` 로 옮긴 선례가 있다 |
| **D2** | **러너가 카탈로그 본문을 읽어 프롬프트에 싣는다** (경로만 가리키지 않는다) | 카탈로그는 규율이 아니라 **DoD 가 걸린 판정 입력**이다. 스킬은 못 읽으면 「문서는 그럴듯하고 규율만 빠지는」 정도지만, 카탈로그를 못 읽으면 **「이미 있음」이 조용히 안 나온다** — 그것이 이 Phase 의 완료 조건이다. 이 저장소는 프롬프트 지시가 형식적으로만 지켜진 것을 **세 번** 확인했다(§11.1) |
| **D3** | **비용 · 세금 · 슬리피지가 5번 칸에 섞여 들어오면 «계측만» 남기고 통과시킨다** | 게이트로 막으면 **금지어 = 기각** 구조가 되어, §10.1 A 가 실측으로 뒤집은 함정에 다시 선다(진짜 원장으로 재 보니 후보 13개 중 12개가 사전에 걸렸다). 기각의 대가가 오탐 중 가장 크고, 이 혼입은 「없는 출처」와 달리 **읽으면 사람이 바로 본다.** 판정(2번 칸)이 아직 없으므로 **그 값이 결론을 만들 자리도 아직 없다.** 분포를 보고 Phase 5 가 판단한다 — §10.1 D(겹침 계측)와 같은 축 |
| **D4** | **진짜 밤을 완주시켜 DoD 를 재고, 같은 밤에 §10.1 F 를 닫는다** | 「넣으면 '이미 있음'으로 나온다」는 밤을 돌려야 나오고, §10.1 F 가 「다음에 밤을 돌리는 Phase 가 곁다리로」라고 지정했다. 단계만 따로 돌리면 F 가 또 미뤄지고 **「이것만을 위해 밤을 태우지 않기로 했다」는 판단이 Phase 5 에서 같은 자리에 다시 선다** |

### 🔴 가정 셋 — 틀리면 결론이 바뀌므로 명시한다

1. **`STEPS` 에 이름을 더하면 «이미 완주한» 예전 폴더가 이어받아진다.** `settled` 가 네 단계인 폴더는 `next_step` 이 `feasibility` 를 돌려주므로 「미완성」으로 보인다. 그 폴더에는 그 밤의 후보가 박혀 있어 **`CANDIDATE_STEPS` 갈래로도 걸러지지 않고**, 이어받으면 **이미 「판 것」으로 표시된 후보를 두고 feasibility 만 도는 밤**이 된다. Phase 2 가 같은 자리에서 같은 일을 겪었다(§11.2)
   - **[정정 2026-09-14 · 사용자 승인] 해당 폴더는 «하나»다.** `runs/20260912_1444` 는 후보가 안 박혀 있어 기존 갈래가 이미 걸러낸다. `runs/20260912_2034` 만 네 단계 완주 + 후보 박힘이다
   - **[정정 2026-09-14 · 사용자 승인] 가르는 자리는 진입점이 아니라 `night._skip_reason` 이다.** 가르는 사실이 **「원장에서 이미 판 것인가」**인데 진입점은 원장을 안 읽고(기존 갈래 셋이 전부 «상태»만 본다) `_skip_reason` 은 이미 읽는다. 그 함수의 질문이 바로 「이 단계를 돌릴 이유가 있나」다. **`--run-dir` 로 지목한 경우와 사람이 손으로 `- [x]` 로 바꾼 경우까지 같은 갈래로 덮인다.** 대가는 그 폴더를 닫는 밤 하나가 리서치를 안 하는 것이고, **에이전트를 부르지 않으므로 비용은 0** 이다
2. **feasibility 는 판정하지 않는다.** 4·5번 칸은 「묻는 자리」이고 기각 신호(신호가 연 1회 · 지수만 있고 ETF 없음)를 **적기만** 한다. 따라서 단계를 앞에 두어 아낄 비용이 없고, D1 의 「마지막」이 성립한다
3. **카탈로그 본문을 실어도 단계 비용이 크게 안 뛴다.** 카탈로그는 「무엇을 받을 수 있나」만 담는 요약이고 자세한 것은 원본을 가리킨다. → 실측(Phase 3)에서 `input_tokens` 로 잰다. 크게 뛰면 그때 항목을 후보의 시장에 따라 골라 싣는 것을 검토한다 (지금 하지 않는다 — 겪지 않은 문제다)

## 4) 완료 조건(Definition of Done)

> Done은 "서술"이 아니라 "체크리스트 상태"로만 판단합니다. (정의/예외는 `/impl-plan` 스킬)

- [x] 기능 요구사항 충족 — 밤이 다섯 단계로 돌고, 후보 폴더에 4·5번 칸 산출물이 생기며, 필수 자리가 비면 그 밤이 미완성으로 끝난다
- [x] 🔴 **옵션 만기일 후보에서 4번 칸의 데이터 판정이 「이미 있음」으로 나온 것을 실측으로 확인** (§10 이 정한 이 Phase 의 검증 방법) — `runs/20260914_1308` · `availability = 이미 있음`
- [x] 🔴 **§10.1 F 실측** — 반증이 도는 동안 컨테이너를 죽였다가(종료 코드 137) 다시 돌려 `[이어받기]` 가 뜨고 `rebut` 부터 도는 것, 그리고 **반쯤 쓰인 파일이 완성본 자리에 남지 않는 것**을 확인
- [x] 회귀/신규 테스트 추가 — 품질 검증에서 **336건** 통과.
      (착수 «전»의 수는 재지 않았으므로 적지 않는다. 판단이 필요하면 직전 실행과 비교한다 — `docs/COMMANDS.md` 가 「`passed` 의 절대값을 기준선으로 삼지 않는다」고 정해 둔 자리다)
- [x] 코드 리뷰 실행 및 결과 기록 (마지막 Phase 의 Validation 에 적는다)
- [x] 품질 검증 통과 (마지막 Phase 의 Validation 에 passed/failed/skipped 수를 적는다)
- [x] 자동 포맷 적용 완료 (마지막 Phase에서 실행)
- [x] 필요한 문서 업데이트(`docs/COMMANDS.md` / CLAUDE.md / plan 등 — 각각 변경 여부 명시)
- [x] 근거 승격 완료 — 이 계획서를 지금 삭제해도 잃을 정보가 없다
      (결정 근거·실측 수치를 루트 `CLAUDE.md` 의 프로젝트 설정 절이 정한 목적지로 이관.
      `/impl-plan` 스킬 "근거 승격" 참고)
- [x] plan 체크박스 최신화(Phase/DoD/Validation 모두 반영)

## 5) 변경 범위(Scope)

### 변경 대상 파일(예상)

**신규**

- `docs/DATA_CATALOG.md` — 데이터 카탈로그. 항목마다 `id` · 자립 서술 · 함정 · 원본 포인터
- `src/research_lab/gate/feasibility.py` — 4·5번 칸의 필수 자리 존재 검사 + 비용 표현 계측 함수
- `src/research_lab/runner/feasibility.py` — 카탈로그 싣기 · 프롬프트 · 게이트 적용 · 산출물 저장 · 「판 것」 표시
- `tests/test_gate_feasibility.py`
- `tests/test_feasibility.py`

**수정**

- `src/research_lab/runner/steps.py` — `STEPS` 에 `feasibility` 추가 · `CANDIDATE_STEPS` 에 추가
- `src/research_lab/runner/lineage.py` — `mark_explored` 제거 (표시가 마지막 단계로 이동) · 모듈 도입부 주석 갱신
- `scripts/run_night.py` — `execute` 분기 추가 · 예전 실행 폴더(네 단계 완주)를 건너뛰는 갈래
- `src/research_lab/common_constants.py` — 카탈로그 경로 · 산출물 파일명
- `.claude/skills/night-research/SKILL.md` — 「러너가 기계로 검사하는 것」 표에 4·5번 칸 줄 추가 · 단계별 절에 `실현가능성` 추가 · description 갱신
- `ledger/원장.md` — 실측용 옵션 만기일 후보 한 줄 (맨 위)
- `docs/INDEX.md` — 카탈로그 등록 (`tests/test_index.py` 가 대조한다)
- `docs/DESIGN.md` — §10 로드맵 Phase 4 줄 완료 · §10.1 머리의 표와 **F 본문 둘 다** · §11.5 신설(이 Phase 의 확정 사항과 실측) · **§7.0 의 「미국 쪽은 아직 실측이 없다」 정정**
- `src/research_lab/CLAUDE.md` — 계층 구조의 `gate/` 설명에 실현가능성 추가 · 계약 §4 의 「마지막 단계가 표시한다」가 가리키는 단계 갱신
- 기존 테스트 — `tests/test_runner_night.py` · `tests/test_runner_resume.py` · `tests/test_run_night_entrypoint.py` · `tests/test_rebut_and_lineage.py` (단계 수와 「마지막 단계」 가정을 담고 있다)
- `docs/plans/PLAN_phase4_feasibility.md` — 이 문서

**`docs/COMMANDS.md`: 변경 없음** — 종료 코드를 늘리지 않고(「기각은 실패가 아니다」와 같은 축), CLI 플래그도 바뀌지 않는다. 단계가 하나 늘어도 실행 명령은 같다. 🔴 **실측에서 이 전제가 깨지면(예: 카탈로그 경로를 인자로 받아야 한다면) 그때 함께 갱신한다.**

### 데이터/결과 영향

- **후보 폴더에 산출물 한 종이 늘어난다** — 4·5번 칸을 담는 JSON. 기존 파일(`찬성근거.json` · `검색어.json` · `반증.json` · `반증검색어.json` · `계보.json`)은 모양이 바뀌지 않는다
- **상태 파일의 `settled` 에 이름 하나가 늘어난다.** 예전 폴더와의 호환은 위 「가정 1」이 다룬다
- **결정 로그에 새 이벤트 필드가 늘어난다** — 카탈로그 적중(`catalog_hit`) · 비용 표현 계측. 기존 필드는 그대로다
- **원장에 후보 한 줄이 늘고, 실측 뒤 그 줄이 `- [x]` 가 된다**
- **기존 결과 비교는 필요 없다** — 이 저장소는 수치를 재지 않는다

## 6) 단계별 계획(Phases)

### Phase 0 — 계약을 테스트로 먼저 고정 (레드 허용)

> 「무엇이 맞는가」를 구현 전에 고정한다. 특히 **단계를 늘릴 때 예전 상태 파일을 어떻게 다루나**는
> Phase 2 에서 실제로 밤을 헛돌게 만든 자리라, 구현보다 먼저 테스트로 박는다.

**작업 내용**:

- [x] `tests/test_gate_feasibility.py` — 게이트 계약
  - [x] 4번 칸: 필요한 데이터 · 받는 방법 · 막히면 무엇으로(대체 사다리) 중 **하나라도 비면 사유를 돌려준다**
  - [x] 4번 칸: 데이터 판정이 **정해진 값 집합** 밖이면 사유를 돌려준다 (값 자체를 판정하지 않고 **적혔는가와 그 값이 약속된 것인가**만 본다)
  - [x] 5번 칸: 살 수 있는 상품 · 신호 빈도 · 배수(**시장을 함께**) · 사람이 깨어 있는 시간에 집행 가능한가 중 하나라도 비면 사유를 돌려준다
  - [x] 🔴 **배수 칸에 시장이 안 적히면 막는다** — 「1배라 기각」이 시장 없이 적히면 **미국의 3배를 확인하기 전에 기각한 것**이 되는데(§7.0), 그 판정은 나중에 되짚을 수도 없다
  - [x] **어떤 입력에도 예외를 올리지 않는다** — 목록 자리에 문자열, `None`, 중첩이 어긋난 값
  - [x] 비용 표현 계측 함수가 **걸린 표현을 돌려주되 판정은 하지 않는다** (D3)
  - [x] 사유 문자열에 **무엇이 왜 비었는지**가 들어간다 — 「거부됨」만 돌리면 다음 밤이 같은 시도를 반복한다
- [x] `tests/test_feasibility.py` — 단계 계약
  - [x] 프롬프트에 **카탈로그 본문이 실린다** (D2). 카탈로그를 못 읽으면 **그 사실을 로그에 남기고 «통과»시킨다** — 판정을 못 하는 것과 실패로 판정하는 것은 다르고(계층 계약 §3), 입력이 없다고 밤을 멈추면 검사기가 파이프라인을 세우는 구조가 된다
  - [x] 게이트가 막으면 **산출물 파일이 생기지 않고** 「판 것」 표시도 안 된다
  - [x] 🔴 **「판 것」 표시를 이 단계가 한다** (D1). `lineage` 는 하지 않는다
  - [x] 출처 URL 이 있으면 **`url_check` 를 탄다** — 값싼 게이트가 전부 통과한 «뒤»에 부른다 (계층 계약 §6)
  - [x] 그 밤의 후보가 없으면 러너가 건너뛴다 (`CANDIDATE_STEPS`)
  - [x] 카탈로그 적중과 비용 표현 계측이 **게이트에 막혀 끝나도** 결정 로그에 남는다
- [x] 이어받기 계약 — **자리가 진입점이 아니라 `night._skip_reason` 으로 바뀌었다** (아래 진행 로그 · 승인 대기)
  - [x] 🔴 **박힌 후보가 원장에서 이미 「판 것」이면 마지막 단계를 건너뛴다** — `tests/test_runner_night.py`
  - [x] 그 판정에 쓸 원장 조회(`ledger.status_of`)가 **정규화를 지난다** — `tests/test_ledger.py`
  - [x] 후보가 안 박힌 미완성을 건너뛰는 기존 갈래는 그대로다 — `tests/test_run_night_entrypoint.py` 무변경
- [x] `tests/test_index.py` 는 고치지 않는다 — 카탈로그를 `docs/INDEX.md` 에 등록하면 통과한다. **등록을 빠뜨리면 이 테스트가 잡는다** (그러라고 있는 장치다)

---

### Phase 1 — 데이터 카탈로그를 쓴다 (그린 유지 · 문서만)

> **들어오는 지식이라 링크를 쓴다.** 자세한 방법은 원본을 가리키고 여기엔 「무엇을 받을 수 있나」만 남긴다.

**작업 내용**:

- [x] `docs/DATA_CATALOG.md` 를 만든다. 항목마다 넷을 담는다
  - [x] **`id`** — 에이전트가 「무엇을 근거로 삼았나」를 가리키는 이름. 계측(`catalog_hit`)이 이 값을 받는다
  - [x] **자립 서술** — dossier 4번 칸에 그대로 옮겨도 **저장소 밖에서 읽히는** 한두 문장. 「`storage/market/` 에 있다」가 아니라 **「국내 ETF 일봉은 pykrx 로 받을 수 있고 이 기계에 받아둔 것이 있다」**
  - [x] **함정** — 그 데이터를 믿고 재면 조용히 틀리는 자리
  - [x] **원본 포인터** — 실측이 적힌 자매 저장소 문서. 🔴 **경로에 한글이 있어 마크다운 링크를 만들지 않고 인라인 코드로 적었다** — VSCode 확장이 퍼센트 인코딩을 디코드하지 않아 클릭해도 아무 일이 안 일어난다(전역 규칙). 머리말에 **「이 줄은 dossier 에 옮기지 않는다」**를 박아 자립 서술과 갈랐다
- [x] 담을 항목 (씨앗 4건 + 시세 파일 실측에서 뽑는다) — **열 항목으로 나왔다**
      (`krx-daily` · `krx-adjusted` · `krx-survivorship` · `us-daily` · `krx-etn` · `krx-futures` ·
      `expiry-calendar` · `distribution-dates` · `instrument-universe` · `leverage-limits`)
  - [x] 국내 시세(일봉) `krx-daily` · 수정주가 `krx-adjusted` — 🔴 **조회 시점 기준 최근 약 3,000거래일만 돌아오고 그 시작점조차 고정이 아니다**
  - [x] 미국 시세(일봉) `us-daily` — 원본가와 수정주가 쌍으로 받아둔 것이 있다
  - [x] 국내 선물(계약별) `krx-futures` — 거를 것이 넷이고 **넷 다 조용히 틀리게 만든다**
  - [x] ETN 시세 `krx-etn` — 🔴 **pykrx 가 주지 않고, 티커가 아니라 ISIN 으로 조회한다**
  - [x] **만기 달력** `expiry-calendar` — 🔴 **외부에서 받는 데이터가 아니라 달력 규칙이다.** 휴장 앞당김 판정에 필요한 거래일 목록은 **시세 파일의 날짜 인덱스 자체**이고, 「달력상 하루 전」으로 구현하면 틀린다
  - [x] **배당락 · 분배락** `distribution-dates` — 만기 창 안에 들어와 결과를 오염시키는 방식이 시장마다 다르다
  - [x] **생존편향(상장폐지 종목)** `krx-survivorship` — 빠져 있으면 결과가 좋게 나온다. 틀리게. 🔴 **2단(개별 조회)에서 되살아나는 자리까지 적었다**
  - [x] **5번 칸 몫 둘** `instrument-universe` · `leverage-limits` — 「살 수 있는 상품이 있나」와 「배수를 걸 수 있나」는 4번 칸이 아니라 5번 칸의 물음이라 따로 두었다
- [x] 🔴 **각 항목의 「가변 정보」를 적지 않는다** — 받아둔 종목 수·파일 개수·거래일 수 같은 값은 자매 저장소가 더 받으면 틀려진다. **종류**로 적고 수치는 원본 포인터에 맡긴다
- [x] 🔴 **비용·세금·슬리피지를 카탈로그에서 «빼 왔다».** 원본 문서에는 총보수·제비용이 표로 적혀 있는데, 그대로 실으면 **프롬프트가 5번 칸에 금지된 값을 손에 들려 주는 셈**이 된다. 뺀 사실과 이유를 머리말에 적었다
- [x] **비어 있는 축을 「비어 있다」고 적는다** — §6 절에 표로 두었다. 없는 데이터와 실측이 없는 것을 구별해야 4번 칸에서 「카탈로그에 없다 = 없는 데이터」로 오판하지 않는다(§7.0)
- [x] `docs/INDEX.md` 에 등록한다 — `tests/test_index.py` **11건** 통과로 확인
      (처음 이 줄에 12건이라 적었는데 실제 수집은 11건이다. 파일은 HEAD 와 한 글자도 다르지 않고
      파라미터 목록이 상수라 **수가 바뀔 수 없다** — 옮겨 적을 때 틀린 것이다.
      「숫자는 도구 출력을 그대로 붙여넣는다」를 어긴 자리)

---

### Phase 2 — feasibility 단계를 붙인다 (그린 유지)

**작업 내용**:

- [x] `src/research_lab/gate/feasibility.py` — Phase 0 의 게이트 계약을 통과시킨다 (36건 통과)
  - [x] 「값을 받아 사유를 돌려준다」 (계층 계약 §5). 파일을 열지 않는다
  - [x] 비용 표현 계측 함수는 `gate/quantified.triggered_terms` 의 선례를 따른다 — 표현을 돌려주되 **판정하지 않는다**. 🔴 **「환율」을 사전에 넣지 않았다** — 환전 스프레드는 비용이지만 환율 «노출»은 10번 칸 기준선 문제다(§7.0)
- [x] `src/research_lab/runner/feasibility.py` (16건 통과)
  - [x] 카탈로그를 읽어 프롬프트에 싣는다. 못 읽으면 로그에 남기고 진행한다
  - [x] 🔴 **카탈로그를 못 읽었을 때 프롬프트에 «빈 문자열을 끼우지 않는다».** 그러면 에이전트가 「이미 밝혀진 것이 하나도 없다」로 읽어 **받을 수 있는 데이터까지 「없다」로 적는다** — §7.0 이 경고한 오판이다. 대신 「없는 데이터라는 뜻이 아니다」를 명시한 문구를 끼운다
  - [x] 프롬프트가 **4·5번 칸에서 묻는 것과 «묻지 않는 것»을 함께** 적는다 — 비용·세금·슬리피지는 묻지 않는다
  - [x] 🔴 프롬프트가 **「카탈로그에 있다」로 적지 말라고 명시하고 자립 서술의 예를 박는다.** 게이트가 못 막는 자리라 프롬프트와 계측이 전부다
  - [x] 결정 로그: 읽은 것(카탈로그 항목 수 · 적중 `id`) → 비용 → 게이트 → 판정. **기록을 게이트 «앞»에서** 남긴다
  - [x] 🔴 **적중 `id` 가 카탈로그에 실제로 있는지 확인해 «없는 id» 를 따로 센다 — 막지 않고 계측만 한다.** 지어낸 `id` 가 섞이면 [미검증] 3 의 계측이 「카탈로그를 썼다」로 **거짓 긍정**을 내고, 그 고장은 에러를 내지 않는다. 막지 않는 이유는 이것이 판정이 아니라 **계측의 진실성**이기 때문이다 — 게이트를 세우면 카탈로그 항목명을 못 옮겨 적은 밤이 통째로 죽는다. **산출물에도 실재하는 이름만 싣는다** (지어낸 이름을 남기면 저장소 밖에서 그것을 찾으러 간다)
  - [x] `url_check.assert_sources_exist` 를 값싼 게이트 통과 뒤에 부른다
  - [x] `atomic_write` 로 산출물을 쓴다
  - [x] 🔴 **파일을 쓴 «뒤에» `ledger.mark_explored` 를 부른다** — 순서가 반대면 산출물 없이 후보가 「판 것」으로 남는다
  - [x] **검색어 게이트를 걸지 «않았다»** — 카탈로그가 답을 주는 후보는 검색이 필요 없고, 하한을 요구하면 **억지 검색을 시키게 된다.** 계보에 검색어 게이트가 없는 것과 같은 이유다. 던졌으면 기록만 한다
- [x] `src/research_lab/runner/steps.py` — `STEPS` 마지막에 추가 · `CANDIDATE_STEPS` 에 추가 (이 단계는 그 밤의 후보를 봐야 한다)
- [x] `src/research_lab/runner/lineage.py` — `mark_explored` 와 `ledger_path` 인자를 걷어냈고 모듈 도입부를 「표시하지 않는다 · 그래서 원장을 받지 않는다」로 갱신했다
- [x] `scripts/run_night.py` — `execute` 분기 추가 · `lineage` 호출에서 원장 제거
- [x] 🔴 **예전 폴더를 가르는 갈래는 `night._skip_reason` 에 넣었다** (진입점이 아니다 — 위 진행 로그의 사유). `ledger.status_of` 를 새로 만들어 **정규화를 원장 쪽에 두었다**
- [x] `src/research_lab/common_constants.py` — `DATA_CATALOG_PATH`(`DOCS_DIR` 의 첫 실사용처) · `FEASIBILITY_FILENAME`
- [x] `.claude/skills/night-research/SKILL.md` — 게이트 표에 줄 둘 추가 · **「막지 않고 세는 것」 절 추가** · 「실현가능성」 단계 절 추가 · frontmatter `description` 갱신
  - [x] 🔴 **컨테이너는 전역 `~/.claude` 를 마운트하지 않으므로 여기 안 적힌 규율은 밤에 존재하지 않는다.** 4·5번 칸 규율을 이 파일에 적었다
- [x] 기존 테스트에서 단계 수·「마지막 단계」 가정을 고친다 — 🔴 **리터럴 목록을 `steps.STEPS[1:]` 로 바꿔 다음에 단계를 늘릴 때 또 안 깨지게 했다.** 계보 테스트의 「판 것」 표시 계약은 **지우지 않고 「표시하지 «않는다»」로 뒤집어** 남겼다 — 그것이 이번 변경의 핵심이라 계약이 사라지면 다음 사람이 되돌릴 수 있다
- [x] 🔴 **이모지 훅이 한 번 막았다** — 프롬프트 문자열(에이전트가 읽는 산문)에 `🔴` 를 썼는데 그것도 `.py` 안이다. 대괄호 표기로 바꿨다. **우회하지 않았다**

---

### Phase 2.5 — 잠금을 `flock` 으로 갈아탄다 (그린 유지) **[2026-09-14 추가 · 사용자 승인]**

> **F 를 재려면 이것이 먼저다.** 지금은 강제 종료 뒤 잠금 파일이 남아
> **이어받기가 구조적으로 일어나지 않는다.** 근거와 실측표는 위 진행 로그에 있다.

**작업 내용**:

- [x] 계약을 테스트로 먼저 고정한다 (`tests/test_runner_state.py`)
  - [x] 잡은 동안 다른 열기(같은 프로세스라도)가 `AlreadyRunningError` 를 받는다
  - [x] 🔴 **강제 종료된 홀더의 잠금이 다음 호출에서 «잡힌다»** — 자식 프로세스를 `SIGKILL` 해 실제로 잰다
  - [x] 🔴 **잠금 파일이 남아 있어도 「잠김」이 아니다** — 파일 존재로 판정하면 그 파일 하나가 폴더를 영구히 잠근다
  - [x] `is_locked` 가 «지금 잡고 있는지»를 돌려준다 (파일이 없으면 False)
  - [x] 잠금 파일을 **지우지 않는다** — 다른 프로세스가 fd 를 든 채 unlink 되면 지워진 inode 를 잠그는 경쟁이 생긴다
  - [x] **판정을 못 하면 「잠김」으로 떨어뜨린다** (권한 오류 등). 훔쳐서 두 밤이 한 폴더를 쓰는 것보다 한 밤을 미루는 쪽이 낫다
- [x] `src/research_lab/runner/state.py` — `fcntl.flock` 으로 갈아타고 `is_locked` 를 낸다. **파일 내용은 쓰지 않는다** (`runs/` 가 git 에 들어가므로 매 밤 바뀌면 diff 소음이 된다)
- [x] `scripts/run_night.py` — 파일 존재 검사를 `state.is_locked` 로 바꾼다. **판정을 한 곳에 둔다**
- [x] 기존 잠금 테스트를 「파일을 써 둔다」에서 **「진짜로 잠는다」**로 고친다 — 파일을 써 두는 것은 이제 잠금이 아니다
- [x] 🔴 **못 막는 것을 문서에 적는다** — flock 은 **host↔container 경계를 넘지 않는다**(실측 ④). 개발용 호스트 실행과 컨테이너 밤이 동시에 돌면 막히지 않는다

---

### Phase 3 (마지막) — 실측 · 문서 정리 · 최종 검증

> **실측을 코드가 그린인 상태에서 한다.** Phase 3(URL 실재)에서 **테스트로는 안 나오는 결함**
> (한글 URL 이 통째로 빠져나갔다)이 실물 계측에서만 잡혔다. 이번에도 재고 나서 문서를 닫는다.

**작업 내용**

- [x] **실측 준비** — `ledger/원장.md` 맨 위에 옵션 만기일 후보를 넣는다 (달력만 보면 값이 정해지는 한 줄 주장)
- [x] 🔴 **밤을 완주시킨다** (컨테이너 · `docs/COMMANDS.md` 의 운용 명령) — `runs/20260914_1308` · 종료 코드 0
  - [x] 4번 칸의 데이터 판정이 **「이미 있음」**으로 나오는지 확인 — **나왔다**
  - [x] 카탈로그 적중이 만기 달력·시세 항목을 가리키는지 확인 — `us-daily` · `expiry-calendar` · `leverage-limits` · `instrument-universe`, **지어낸 이름 0건**
  - [x] 4번 칸이 **자립 서술로 적혔는지** 사람이 읽어 확인 — **「카탈로그에 있다」가 한 줄도 없다.** 만기 달력이 외부 데이터가 아니라 달력 규칙이라는 것과 거래일 목록이 일봉의 날짜 인덱스로 닫힌다는 것이 그 자리에 풀려 적혔다
  - [x] 5번 칸에 비용 표현이 섞였는지 계측값으로 확인 — **`cost_terms = []`**
  - [x] feasibility 한 단계의 **비용 · 소요 시간 · 토큰** — **$0.3511 / 135.1초 / 49,659 토큰.** 🔴 **카탈로그 7KB 를 실었는데 가장 싼 단계 중 하나였다** — 단계 수를 늘렸는데 성공 비용이 §11.2 의 $2.24 에서 $1.9609 로 줄었다
  - [x] **곁가지 수확 — 5번 칸이 문제를 잡았다.** 「한국시간 야간이라 사람이 지켜본다면 통상적인 시간이 아니다 — 예약 주문이면 되지만 증권사별 지원은 확인 못 했다」가 나와 미검증으로 갔다
- [x] 🔴 **§10.1 F — 강제 종료 실측** (같은 밤의 곁다리)
  - [x] 수집이 끝난 것을 결정 로그에서 본다 (`collect judged`) — 🔴 **감시 조건을 두 번 고쳐야 했다**(위 진행 로그)
  - [x] 반증이 도는 동안 컨테이너를 죽인다 — 종료 코드 **137**
  - [x] 다시 돌려 `[이어받기] 미완성을 찾았습니다` 가 뜨고 **`rebut` 부터** 도는지 본다 — **떴다.** Phase 2.5 의 수정이 없었으면 구조적으로 불가능했다
  - [x] **반쯤 쓰인 파일이 완성본 자리에 남지 않았는지** 확인 — `반증.json` 이 아예 없고 수집 산출물만 있었다. ※ 강제 종료가 쓰기 «도중»에 떨어진 것은 아니다(창이 밀리초)
- [x] 🔴 **예전 폴더가 실제로 건너뛰어지는지** 확인 (가정 1) — 해당 폴더는 **하나**였고(`20260912_2034`), 첫 밤이 그것을 이어받아 마지막 단계를 사유와 함께 건너뛰고 닫았다. **에이전트를 한 번도 안 불렀다 (비용 0)**
- [x] 필요한 문서 업데이트
  - [x] `docs/DESIGN.md` §10 로드맵 — Phase 4 줄에 완료 표시 · Phase 5 에 「다음」 표시
  - [x] 🔴 `docs/DESIGN.md` §10.1 — **머리의 표와 F 본문 «둘 다»**. F 본문은 지우지 않고 제목에 완료 표시만 달고, **재 보니 이어받기가 안 되고 있었다**는 것을 그 자리에 적었다
  - [x] `docs/DESIGN.md` §10.1 — 새로 드러난 개선거리 **H · I · J** 를 어느 Phase 가 맡을지와 함께 적었다 (표와 본문 둘 다)
  - [x] `docs/DESIGN.md` §11.5 신설 — D1~D4 · 표시 이동 · 「안 판 것」 조건 · 잠금 실측표 · 카탈로그 주입 · 게이트 축 · 계측 · 못 막는 자리 · 수치
  - [x] 🔴 `docs/DESIGN.md` §7.0 — 「미국 쪽은 아직 실측이 없다」를 **정정**했다
  - [x] `src/research_lab/CLAUDE.md` — 계층 구조 · 계약 §4(표시가 단계를 따라 옮겨간다 · 「안 판 것」 조건) · **계약 §7 신설**(잠금 판정은 `state` 가 한다)
  - [x] `docs/INDEX.md` — `DATA_CATALOG.md` 등록 확인
  - [x] `docs/COMMANDS.md` — **변경 없음** (잠금·종료 코드·플래그를 언급하지 않아 `grep` 으로 확인)
  - [x] `.claude/skills/night-research/SKILL.md` — 게이트 표 · 실현가능성 절 · 🔴 **§0 을 「파일을 쓰는 것은 러너다」로 바로잡았다**(실측으로 찾은 $0.58 결함)
  - [x] `.gitignore` — 잠금 파일 제외 (코드 리뷰 지적)
- [x] 자동 포맷 적용 (`poetry run black .`) — 1개 파일 재포맷
- [x] 변경 기능 및 전체 플로우 최종 검증 — 밤 완주 + 강제 종료 이어받기 + 예전 폴더 닫힘을 **컨테이너에서** 확인
- [x] 근거 승격 — 「이 계획서를 지금 지워도 잃는 것이 없는가?」
- [x] DoD 체크리스트 최종 업데이트 및 체크 완료
- [x] 전체 Phase 체크리스트 최종 업데이트 및 상태 확정

**Validation**:

> 순서를 지킨다 — 리뷰에서 고치면 코드가 바뀌므로 품질 검증이 마지막 관문이어야 한다.

- [x] `/code-review xhigh` (발견 11건 · 조치: **10건 고침 · 1건은 다른 수정이 흔한 경로를 덮어 부분 해소**)
- [x] `poetry run python validate_project.py` (passed=336, failed=0, skipped=0)

#### Commit Messages (Final candidates) — 5개 중 1개 선택

> **Done 직전에 `/commit` 을 실행해 이 절을 채운다. 그전에는 비워 둔다.**
> 계획서를 쓰는 시점에는 diff 가 없어 여기 적는 것은 전부 추측이고,
> **추측으로 적은 줄은 그대로 나간다.** 형식·문체 규칙은 `/commit` 이 정한다.

(비어 있음 — `/commit` 으로 채운다)

## 7) 리스크(Risks)

| # | 리스크 | 완화 |
| --- | --- | --- |
| 1 | 🔴 **`STEPS` 를 늘려 예전 폴더가 이어받아진다.** 이미 「판 것」으로 표시된 후보를 두고 feasibility 만 도는 밤이 되고, **에러는 안 난다** | 진입점이 건너뛰게 만들고 **Phase 0 에서 테스트로 먼저 박은 뒤** Phase 3 에서 **실제 폴더 둘로** 확인한다. Phase 2 가 같은 자리에서 겪은 일이다 |
| 2 | **에이전트가 4번 칸에 「카탈로그에 있다」로 적어 dossier 독립성이 깨진다** | 게이트가 못 막는다(내용 판정이 된다). 프롬프트에 자립 서술의 예를 박고 `catalog_hit` 를 계측으로 남긴다 — **아침에 사람이 보는 자리**다. Phase 3 실측에서 사람이 읽어 확인한다 |
| 3 | **카탈로그를 실어 프롬프트가 커지고 단계 비용이 뛴다** | 카탈로그는 요약만 담는다. Phase 3 에서 `input_tokens` 로 재고, 크게 뛰면 그때 시장별로 골라 싣는 것을 검토한다 (지금 하지 않는다) |
| 4 | **강제 종료 실측(F)이 예상과 다르게 나온다** — `atomic_write` 가 반쯤 쓴 파일을 막는 것이 실제로는 안 될 수 있다 | 그때가 처음이므로 **원문을 통째로 남긴다.** 분류표를 원문으로 가르친 것과 같은 방식이고, 틀어졌다면 그것이 이 Phase 의 가장 큰 수확이다 |
| 5 | **밤이 한도·인증에 걸려 실측을 못 끝낸다** | 한도는 §9 의 ①「정상」이라 다음 밤이 이어받는다. DoD 가 안 닫히므로 **Done 처리하지 않고** 다음 밤을 기다린다 |
| 6 | **카탈로그가 자매 저장소의 실측과 어긋나게 낡는다** | 항목에 **가변 정보를 적지 않고** 원본 포인터를 단다. 링크가 파이프라인 안에 머물러 살아 있다 |
| 7 | **5번 칸의 필수 자리를 늘려 멀쩡한 밤이 막힌다** | 존재 검사만 하고 **질은 안 본다.** 자리를 넷으로 묶고, 「없음」·「해당 없음」이라 적을 길을 남긴다 — 게이트 원형이 「빈 경우에도 적을 길을 남긴다」로 정한 것과 같다 |
| 8 | 🔴 **막힘에 걸리는 자리가 하나 늘어난다.** feasibility 가 3밤 막히면 그 후보가 `- [!]` 로 걷어내지는데, **그때까지 산 수집·반증·계보($2.24)가 함께 버려진다** | 모양 자체는 새로운 것이 아니다 — 계보가 마지막이던 동안에도 계보가 막히면 수집·반증($1.77)이 함께 버려졌다. **늘어난 것은 노출이지 구조가 아니다.** 완화는 게이트를 «싸게» 두는 것이다: 존재 검사만 하고, url_check 를 값싼 게이트 뒤에 두어 대부분의 막힘이 **에이전트를 부르기 전이나 부른 직후**에 드러나게 한다 |

## 8) 메모(Notes)

### 🔴 DESIGN §7.0 의 「미국 축은 비어 있다」를 정정해야 하는 근거

§7.0 이 **「미국 쪽은 아직 실측이 없다」**고 적고 「지금 채우지 않는다」로 닫아 두었다.
**그 서술이 사실과 어긋난다.**

| §7.0 이 적은 것 | 실제로 확인된 것 |
| --- | --- |
| 「§5.1 의 씨앗은 전부 국내다」 | **맞다.** §5.1 표의 네 줄은 pykrx · ETN · 국내 선물 · 만기/배당락이다 |
| 「미국 쪽은 아직 실측이 없다」 | 🔴 **틀리다.** 미국 ETF 일봉을 yfinance 계열로 받아 **원본가와 수정주가 쌍으로 받아둔 것이 있고**, 수정주가 교차검증 실측이 옵션 만기일 설계 §7.6 에 있다. 만기일 달력·배당락 실측도 QQQ 기준으로 들어 있다 |

**정확한 서술은 「미국 축의 카탈로그 항목을 아직 정리하지 않았다」**이며, 「실측이 없다」와는 다르다.
전자는 이번 Phase 가 하는 일이고 후자는 **하면 안 되는 일**(겪지 않은 문제를 푸는 것)이다.

> **왜 이 구별이 중요한가**: 「실측이 없다」를 믿으면 미국 후보의 4번 칸에서
> **이미 받아둔 데이터를 「없다」로 적게 된다.** 그것은 §7.0 자신이 경고한
> 「카탈로그에 없다 = 없는 데이터」 오판과 **같은 고장을 반대 방향에서** 내는 것이다.

### [미검증] 목록과 «어떻게 잴 것인가»

| # | 모르는 것 | 어떻게 잴 것인가 |
| --- | --- | --- |
| 1 | **feasibility 한 단계의 비용·소요 시간** | Phase 3 실측에서 결정 로그의 `cost` 이벤트로 잰다. §11.2 의 단계별 값(수집 $0.79 · 반증 $0.98 · 계보 $0.47)과 나란히 §11.5 에 적는다 |
| 2 | **카탈로그 본문을 실은 프롬프트의 토큰 증가분** | 같은 자리에서 `input_tokens` 로 잰다. 🔴 `cache_read_input_tokens` 를 더하지 않는다 — 캐시가 도는 상황에서 그 값은 한 자릿수 크고, 그대로 더하면 예산 근거가 그 배수만큼 틀어진다 |
| 3 | **에이전트가 카탈로그를 실제로 근거로 쓰는가** | `catalog_hit` 계측. 늘 비어 있으면 **카탈로그를 실어도 안 쓴 것**이라 프롬프트가 문제다 |
| 4 | **5번 칸에 비용 표현이 얼마나 섞이는가** | 비용 표현 계측의 분포. 몇 밤 쌓인 뒤 **Phase 5 가 판단한다** — 지금 임계값을 정하면 겪지 않은 문제를 푸는 것이다 |
| 5 | **강제 종료가 반증 자리에서 이어받히는가** (§10.1 F) | Phase 3 에서 반증이 도는 동안 컨테이너를 죽여 잰다. **확인할 것은 하나** — 반쯤 쓰인 파일이 완성본 자리에 남지 않는가 |
| 6 | **4번 칸의 「데이터 판정」 값 집합이 실제 후보를 담기에 충분한가** | 실측에서 에이전트가 집합 밖의 값을 내면 게이트가 사유와 함께 막는다. 그 사유가 「집합이 모자랐다」를 가리키면 집합을 늘린다 — **원문으로 분류표를 가르친 것과 같은 방식** |

### 코드 리뷰가 잡은 것 (발견 11건 · 2026-09-14)

🔴 **가장 무거운 둘은 «고치려던 버그를 권한으로 되살리는» 길이었다.** Phase 2.5 의 첫 구현이
잠금 파일을 **쓰기로** 열었는데, `flock` 은 쓰기 권한을 요구하지 않는다. 다른 uid 가 만든
잠금 파일에서 `PermissionError` 가 나고, 그것이 ① 잠그는 쪽에서는 진입점이 못 잡는 예외로
올라 **이후 모든 밤이 같게 죽고** ② 판정 쪽에서는 「잠김」으로 읽혀 **아무도 잡지 않은 폴더가
영구히 건너뛰어진다.** 둘 다 계약 테스트로 고정했다.

| # | 무엇 | 조치 |
| --- | --- | --- |
| 1 | `lock()` 이 파일을 쓰기로 열어 `PermissionError` 가 `AlreadyRunningError` 를 비켜 간다 | **고침** — `os.open(O_RDONLY \| O_CREAT)` |
| 2 | `is_locked()` 가 쓰기로 열어, 못 열면 「잠김」으로 떨어진다 | **고침** — 읽기로 연다 |
| 3 | `load_catalog` 이 `OSError` 만 잡아 `UnicodeDecodeError` 가 새어 나간다 | **고침** — WSL·mac 을 오가는 한글 문서라 현실적인 갈래다 |
| 4 | 건너뛰기 조건이 「판 것」만 봐서 **기각·막힘 사유 줄이 지워진다** | **고침** — 「안 판 것이 아니면」으로. 원장 줄이 사라진 경우까지 덮인다 |
| 5 | Ruff `UP038` 로 **품질 검증이 실패하고 있었다** | **고침** — `isinstance(x, A \| B)` |
| 6 | `mark_explored` 가 무방비라 원장 줄이 사라지면 상한까지 재시도한다 | **부분** — 4번이 흔한 경로를 건너뛰게 해 막았다. 쓰기 도중 줄이 지워지는 좁은 창은 남는다(예전 계보 단계와 같은 노출) |
| 7 | `is_locked` 가 배타 잠금으로 찔러 **판정기 둘이 서로 부딪힌다** | **고침** — 공유 잠금 |
| 8 | 잠금 파일이 영구 추적된다 | **고침** — `.gitignore` |
| 9 | 잠금 홀더 테스트에 정리가 없어 60초 자는 자식이 남을 수 있다 | **고침** — 컨텍스트 매니저로 |
| 10 | `_skip_reason` 이 원장을 단계마다 두 번 파싱한다 | **고침** — 재고 판정을 쓰는 단계로 한정 |
| 11 | 적중 목록을 거르는 코드가 두 곳에 복제돼 있다 | **고침** — 한 번 묶어 로그와 산출물이 같은 값을 쓴다 |

> **리뷰를 품질 검증 «앞»에 둔 것이 여기서 값을 했다.** 5번 때문에 품질 검증이 이미 실패
> 상태였고, 리뷰를 나중에 돌렸다면 1·2번을 고친 뒤 검증을 또 돌려야 했다.

### 스킵

없음. 스킵 없이 Phase 분해로 해결한다 — Phase 0 이 계약을, Phase 2 가 구현을 맡는다.

### 진행 로그 (KST)

- 2026-09-14 11:47: 계획서 작성. 설계 결정 D1~D4 사용자 승인 완료. 로드맵 §10 의 「아직 안 된 첫 줄」이 Phase 4 이고, §10.1 에서 이 Phase 가 맡는 줄은 **F**(다음에 밤을 돌리는 Phase) 하나임을 확인
- 2026-09-14 13:22: 🔴 **§10.1 F 를 «실제 강제 종료»로 닫았다 — 그리고 Phase 2.5 의 수정이 없었으면 못 닫혔다.**
  - 수집이 끝난 것을 새 폴더의 로그에서 확인(`collect judged` · `opex-week-us` · 근거 3건 · URL 살아 있음 3 · 죽음 0)한 뒤 **반증 호출이 도는 중에 `docker kill`** 했다. 밤은 **종료 코드 137**
  - **디스크 상태**: 잠금 파일이 `runs/.../lock` 과 **`ledger/lock` 둘 다 남았고**, `state.is_locked` 는 **둘 다 False**. 상태 파일은 `settled: [explore, collect]` · 후보 박힘으로 **무손상**. `반증.json` 은 없고 `찬성근거.json` · `검색어.json` 만 있다 — **반쯤 쓰인 파일이 완성본 자리에 남지 않았다**
  - **재실행**: `[이어받기] 미완성을 찾았습니다: /work/runs/20260914_1308` 가 떴다. **예전 구현이면 진입점이 그 폴더를 건너뛰고, 이어서 원장 잠금에 걸려 그 밤이 즉시 멈췄을 것이다**
  - ※ 강제 종료가 «쓰기 도중»에 떨어진 것은 아니다 — 창이 밀리초라 에이전트 호출 중에 떨어졌다. `atomic_write` 의 계약은 계약 테스트가 고정하고 있고, 여기서 확인한 것은 **「끊긴 밤의 폴더에 완성본 흉내를 내는 파일이 없다」**는 쪽이다
- 2026-09-14 13:30: 🔴 **낯선 파일과 수집 실패가 «같은 원인»이었다 — 원문이 남아 있어 닫혔다. 비용 $0.58 을 태웠다.**
  - 첫 밤의 결정 로그에 `collect failed` 가 한 줄 있었다(갈래 `other` · 시도 1/3). 원문의 `result` 전문이 이것이다 —
    「`runs/20260914_1308/opex-week-open-close.collect.json`에 찬성 근거 수집 결과를 저장했습니다. / 요약: …」
  - **인과**: ① 리서치 스킬 §0 이 「**지시받은 경로에 JSON 파일로 쓴다.** 마지막 메시지에 요약을 적는 것으로 갈음하지 않는다」 ② 러너 프롬프트는 「**다른 말 없이 JSON 하나만** 출력하세요」 ③ **에이전트가 ①을 따랐다** — 파일에 쓰고 마지막 메시지에는 경로와 요약을 적었다 ④ 파서가 산문에서 JSON 을 못 찾아 실패 ⑤ `other` 로 분류돼 재시도, 두 번째가 성공
  - **비용**: 실패 시도 **$0.5827** + 성공 **$0.5726** = 이 밤의 수집이 **$1.1553** 이었다. **절반이 이 어긋남에 탔다**
    (🔴 처음 이 줄에 「$0.58 + $0.79 = $1.37」이라 적었다. **$0.79 는 §11.2 의 «다른 밤» 값**인데 이 밤의 로그를 안 보고 끌어다 썼다 —
    전역 규칙 「숫자는 도구 출력을 그대로 붙여넣는다. 눈으로 읽어 다시 타이핑한 값은 근거물이 아니다」를 어긴 자리다)
  - 🔴 **§11.1 의 「에이전트는 「다른 말 없이 JSON 만」을 지키지 않는다」와 «다른» 고장이다.** 그때는 코드펜스로 감싼 것이라 파서를 고쳐 잡았지만, 이번은 **JSON 이 아예 없다.** 파서로는 못 잡는다 — **지시가 서로 어긋난 것이 원인**이라 지시를 고쳐야 한다
  - **간헐적이다** — §11.2 의 밤은 재시도 0회였다. 그래서 그 밤에는 안 드러났다
  - **고치는 쪽은 스킬 §0** 이고, **지금 도는 밤이 그 파일을 읽으므로 밤이 끝난 뒤에 고친다**
  - 곁가지로 얻은 것 둘 — ① 에이전트가 **죽은 URL(404)과 봇 차단(403)을 스스로 근거에서 제외**했다(URL 규율이 게이트 앞에서 이미 작동한다) ② §10.1 G 의 403 이 실물로 또 나왔다(ssrn · sciencedirect · tandfonline)
- 2026-09-14 13:24: 🔴 **실측에서 «예상 못 한» 것이 나왔다 — 에이전트가 산출물을 스스로 파일로 쓴다.**
  - 끊긴 폴더에 `runs/20260914_1308/opex-week-open-close.collect.json` 이 있었다. 러너가 쓰는 파일이 아니다(러너는 `<식별자>/찬성근거.json` 에 쓴다). 내용은 **수집 답 전체의 복제**이고, **식별자가 `opex-week-open-close` 로 원장의 `opex-week-us` 와 다르다**
  - **원인은 지시가 서로 어긋난 것이다** — 리서치 스킬 §0 이 「**지시받은 경로에 JSON 파일로 쓴다.** 마지막 메시지에 요약을 적는 것으로 갈음하지 않는다」고 적었는데, **러너는 경로를 주지 않고 stdout 의 JSON 을 받아 자기가 파일을 쓴다.** 에이전트가 둘을 다 만족시키려고 **경로를 «지어냈다»**
  - **해로운 점 셋**: ① `runs/` 가 git 에 포함되므로 밤마다 쓰레기가 커밋된다 ② 그 파일의 식별자가 원장과 달라 **나중에 읽는 사람이 같은 후보를 둘로 본다** ③ 답을 두 번 쓰므로 토큰을 더 쓴다
  - **고치는 쪽은 스킬이다** — 「산출물은 러너가 파일로 쓴다. 너는 JSON 만 출력한다」로 바로잡는다. 스킬은 이미 이 Phase 의 Scope 에 들어 있다
- 2026-09-14 13:10: **인증 프리플라이트** — 컨테이너에서 `claude -p` 를 한 번 찔러 `is_error: false` 확인. **호출 한 번의 «바닥값»이 $0.095** 였다(입력 2 · 출력 3 토큰). 단계 비용이 대부분 에이전트의 웹 조회에서 오더라도 **호출 자체에 $0.1 가까운 고정비**가 붙는다는 뜻이고, 밤 예산을 정할 때 단계 수가 그만큼 곱해진다
- 2026-09-14 13:08: 🔴 **가정 1 이 실측으로 확인됐다 — 첫 밤이 예전 폴더를 닫았고 에이전트를 «한 번도» 안 불렀다.**
  - `[이어받기] 미완성을 찾았습니다: /work/runs/20260912_2034` → `마친 단계: [... 'feasibility']  건너뛴 단계: ['explore', 'feasibility']` → **종료 코드 0**
  - 결정 로그에 사유가 그대로 남았다 — `feasibility | skipped | 그 밤의 후보가 원장에서 이미 「판 것」이다 — 더 물을 자리가 아니다`
  - **비용 0 이고 그 폴더는 영구히 닫혔다.** 예측한 대가(「그 하루가 리서치를 안 한다」)가 정확히 그대로 나왔다
- 2026-09-14 13:0x: **측정 절차에서 내 실수 둘을 잡았다 — 둘 다 「죽이기 전에 확인」해서 걸렸다**
  - 🔴 감시 조건을 `runs/*/decisions.jsonl` 로 글롭해 **예전 밤의 로그에 걸렸다.** 조건이 처음부터 참이라 즉시 발화했고, 그대로 믿었으면 **수집이 20초밖에 안 돈 컨테이너를 죽였다.** 새 폴더로 좁혀 다시 걸었다
  - 🔴 `rebut` 의 `read` 이벤트는 **반증 호출이 끝난 뒤** 찍힌다(`ask` → 파싱 → 기록 순서). 그때 죽이면 반증이 이미 끝나 `settled` 에 들어가므로 **F 가 요구하는 「`rebut` 부터 도는지」를 못 잰다.** 그래서 `collect judged` 를 창의 시작으로 삼았다
- 2026-09-14 13:28: Phase 2.5 완료 — 테스트 **331건 전부 그린**. 🔴 **원시 도구가 아니라 «진짜 코드»로 컨테이너에서 다시 쟀다**
  - 컨테이너 A 가 `state.lock` 을 잡은 동안 컨테이너 B 의 `state.is_locked` → **True** (정상으로 막힌다)
  - `docker kill` A 뒤 컨테이너에서 → **잠금 파일은 남아 있고**(`lock` 파일 존재 = True) **`is_locked` = False** 이며 **`state.lock` 획득 성공**
  - 즉 **§10.1 F 의 전제가 이제 성립한다** — 죽은 자리에서 다시 돌리면 이어받는다
- 2026-09-14 13:10: **사용자 승인 둘** — ① 잠금 문제를 **이 Phase 에서 고친다** ② 예전 폴더를 가르는 자리는 `night._skip_reason` 로 **유지**한다. 아래 「가정 1」과 Scope 를 그에 맞게 고쳤고 **Phase 2.5 를 새로 뒀다**
- 2026-09-14 13:06: 🔴 **잠금 문제가 처음 본 것보다 넓다 — 그리고 고치는 도구를 «재서» 정했다.**
  - **원장 잠금도 남는다.** `run_night` 이 `state.lock(ledger_path.parent)` 도 잡으므로, 밤이 한 번 죽으면 **이후 모든 밤이 원장 잠금에서 즉시 `AlreadyRunningError` 로 멈춘다.** 실행 폴더 쪽은 조용히 새 밤이 돌지만 이쪽은 **파이프라인이 통째로 선다** — 종료 코드 1 과 stderr 로는 뜨지만 무인 실행에서는 며칠 뒤에나 안다
  - **시각 기준 상한은 이 문제를 못 푼다.** 「죽인 뒤 5분 만에 재실행」과 「5분째 정상 실행 중」이 시각으로는 구별되지 않는다 — F 실측이 정확히 그 모양이다
  - 🔴 **그래서 `flock` 으로 갈아탄다. 커널이 프로세스 종료 시 놓아주므로 상한값이 필요 없다.** virtiofs 를 지나는 자리라 **추론하지 않고 실측했다**(§11.3 에서 권한 비트로 오판했던 것과 같은 실수를 피한다)

    | # | 잰 것 | 결과 |
    | --- | --- | --- |
    | ① | 호스트: 잡은 동안 다른 프로세스가 잡기 | **busy** (정상) |
    | ② | 호스트: 홀더를 `SIGKILL` 한 뒤 | **acquired** — 커널이 놓아준다 |
    | ③ | **컨테이너 A 홀드 → 컨테이너 B** | **busy** (정상) — 같은 VM 커널이라 통한다 |
    | ④ | 🔴 컨테이너 A 홀드 → **호스트** | **acquired** — **경계를 «넘지 않는다»** |
    | ⑤ | **`docker kill` 뒤 컨테이너에서** | **acquired** — 이것이 고치려던 자리다 |
    | — | 잠금 «파일» | **남는다** → 파일 존재가 「잠김」을 뜻하지 않는다 |

  - 🔴 **④ 는 잃는 것이다 — 알고 택한다.** 지금의 O_EXCL 파일 잠금은 파일 존재가 양쪽에 보여 **host↔container 를 막아 준다.** flock 은 못 막는다. 그래도 갈아타는 이유: 막는 대상이 **「사람이 손으로 띄운 개발용 밤 vs 예약된 컨테이너 밤」의 동시 충돌**(낮은 확률 · 사람이 자기 터미널에서 본다)인데, 고치는 대상은 **「컨테이너가 죽으면 파이프라인이 무인 상태로 정지」**(시간이 지나면 반드시 일어남 · 아무도 안 봄)다. **못 막는 것을 문서에 적는다** — §10.1 G 가 URL 게이트의 못 보는 자리를 적어 둔 것과 같은 결이다
  - **잠금 파일을 «지우지 않는다».** 다른 프로세스가 그 파일에 fd 를 들고 있는 동안 unlink 하면 **지워진 inode 를 잠그는** 고전적 경쟁이 생긴다. 파일은 빈 채로 남기고 **내용을 쓰지 않는다** — 매 밤 내용이 바뀌면 `runs/` 가 git 에 포함되므로 diff 소음이 된다
  - **[미검증]** WSL 에서의 flock — 저장소가 WSL 의 Linux 파일시스템에 있으면 표준 동작이지만(§8.1 대로) **재지 않았다.** Windows 드라이브(drvfs)에 두면 다르다
- 2026-09-14 12:52: 🔴 **§10.1 F 를 재기 «전»에 그 자리에서 다른 것이 나왔다 — 사용자 판단 필요.** 돈을 쓰지 않고 잡았다.
  - **[실측 2026-09-14] 강제 종료 뒤 잠금 파일이 남는다.** `SIGKILL`·`SIGTERM` 둘 다 남았다(종료코드 -9 · -15). 재현: 스크래치패드의 `measure_lock_after_kill.py` — 자식이 `state.lock` 을 잡고 부모가 죽인 뒤 파일 존재를 본다. **에이전트를 부르지 않는다**
  - **왜 둘 다인가**: 파이썬은 `SIGTERM` 핸들러를 기본으로 달지 않아 `finally` 가 안 돈다. 게다가 컨테이너 안 PID 1 은 **핸들러 없는 시그널을 무시**하므로 `docker stop` 도 10초 뒤 `SIGKILL` 로 끝난다 — **「얌전히 멈추는」 경로가 없다**
  - **그래서 무슨 일이 나나**: 진입점이 잠긴 폴더를 건너뛰므로(`test_locked_night_is_skipped` 가 이미 고정한 계약) **그 폴더는 영구히 이어받히지 않는다.** 후보는 「판 것」이 안 됐으니 다음 밤이 원장에서 **같은 후보를 다시 꺼내 수집을 다시 산다**(§11.2 실측 $0.79). 버려진 폴더가 쌓이고 **에러도 경고도 없다**
  - 🔴 **설계의 중심 주장과 어긋난다** — §「상태와 로그」가 「한도로 끊겨도 · **컨테이너가 죽어도** · PC 가 재부팅돼도 같은 방식으로 복구됩니다」라고 적어 두었다. 복구가 안 된다
  - 🔴 **그래서 DoD 의 F 항목을 지금 상태로는 «잴 수 없다»** — 「다시 돌려 `[이어받기]` 가 뜨고 `rebut` 부터 도는지 본다」가 **구조적으로 일어나지 않는다.** 고치거나 DoD 를 바꾸거나 둘 중 하나다
  - **잠금의 PID 로는 못 가른다** — 적히는 것이 컨테이너의 PID 1 이라 호스트에서 의미가 없고 실행마다 겹친다
- 2026-09-14 12:40: 실측 준비 — 이미지 빌드 완료(**1.41GB** · 안의 `claude 2.1.270`), 원장 맨 위에 `opex-week-us` 후보 삽입. 그 후보가 **정성 표현 게이트에 걸리지 않고 파라미터 없이 통과**하는 것을 확인했다(달력만 보면 값이 정해진다). 🔴 **`CLAUDE_CODE_OAUTH_TOKEN` 이 없어 밤을 돌릴 수 없다** — 사용자 몫
- 2026-09-14 12:38: [미검증] 2 를 **부분적으로 먼저 쟀다** — 카탈로그 본문 **7,171자**, 실현가능성 프롬프트 **9,176자**(카탈로그 없으면 2,050자). 다른 단계 프롬프트의 몇 배이지만 단계 비용은 대개 에이전트 자신의 웹 조회가 만든다 — 실제 토큰·비용은 밤에서 잰다
- 2026-09-14 12:36: Phase 2 완료 — 테스트 **325건 전부 그린**(레드 0). 실측(Phase 3) 준비만 남았다
- 2026-09-14 12:28: Phase 1 완료 — 카탈로그 아홉 항목. 🔴 **비용·세금·슬리피지를 원본에서 «빼 왔다»** (원본에는 총보수·제비용 표가 있는데, 그대로 실으면 프롬프트가 5번 칸에 금지된 값을 손에 들려 준다)
- 2026-09-14 12:14: 🔴 **Phase 0 에서 가정 1 의 «자리»와 «규모»가 둘 다 달라졌다 — 사용자 승인 대기.** 실제 폴더를 열어 보고 확인한 것이다.
  - **규모**: 영향받는 예전 폴더는 **둘이 아니라 하나**다. `runs/20260912_1444` 는 `settled=["explore","collect"]` 인데 **후보가 안 박혀 있어** 기존 갈래(§11.2 에서 만든 것)가 이미 걸러낸다. `runs/20260912_2034` 만 네 단계 완주 + 후보 박힘이다
  - **자리**: 그 하나를 가르는 사실이 **「원장에서 이미 판 것인가」** 인데, 진입점은 원장을 안 읽는다(기존 갈래 셋이 전부 «상태»만 본다). 반면 `night._skip_reason` 은 **이미 원장을 읽고 있고**(`ledger.next_unexplored`) 「이 단계를 돌릴 이유가 있나」가 바로 그 함수의 질문이다. 그래서 거기로 옮겼다
  - **바뀌는 것**: 진입점이 그 폴더를 이어받고, 마지막 단계를 **사유와 함께 건너뛰어** 폴더를 닫는다. **에이전트를 부르지 않으므로 비용은 0** 이고, 그 밤은 완주로 끝난다 — 대가는 **그 하루가 리서치를 안 한다**는 것 하나다(그 폴더는 영구히 닫히므로 한 번뿐)
  - **왜 이쪽이 나은가**: 진입점에 넣으면 그 하루를 아끼지만 **`_resolve_run_dir` 에 원장을 물려야 해 기존 테스트 9곳의 호출이 바뀌고**, 「상태만 보는」 진입점에 파일 하나가 더 붙는다. 그리고 `_skip_reason` 쪽은 **`--run-dir` 로 직접 지목한 경우와 사람이 손으로 `- [x]` 로 바꾼 경우까지 같은 갈래로 덮는다.** 그 밤의 모양(완주 + 건너뛴 사유는 결정 로그에)은 §11.2 가 「후보를 하나도 못 판 밤」에 이미 정해 둔 것과 같다
  - **아직 안 고친 것**: 위 「가정 1」 본문과 Scope 의 파일 목록. **승인을 받고 고친다** — 조용히 기준을 낮추는 것과 구별하기 위해서다
- 2026-09-14 12:10: Phase 0 완료 — 의도한 레드 4건(`feasibility` 게이트 모듈 없음 · `FEASIBILITY_FILENAME` 없음 · `ledger.status_of` 없음 · 마지막 단계가 안 건너뛰어짐). 나머지 46건은 그린 유지
- 2026-09-14 11:58: 자체 검증(승인 «전»)에서 넷을 더함 — ① 산출물 파일명을 `실현가능성.json` 으로 못박음 ② 없는 카탈로그 `id` 를 계측해 [미검증] 3 의 거짓 긍정을 막음 ③ 리스크 8(막힘 노출이 한 단계 늘어남) ④ 런타임 의존성 0개 유지를 비목표로 명시. 영향받는 기존 테스트 목록은 `grep` 으로 대조해 확인했다 — `test_runner_night.py`(단계 목록이 리터럴로 박혀 있음) · `test_runner_resume.py` · `test_run_night_entrypoint.py` · `test_rebut_and_lineage.py` 넷이고, `test_ledger.py` 는 `ledger.mark_explored` 를 직접 부르므로 **영향받지 않는다**

---
